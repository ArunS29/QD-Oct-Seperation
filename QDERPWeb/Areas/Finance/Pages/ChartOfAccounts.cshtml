@page
<style>
    .content-container {
        background-color: #c0dceb;
        border-radius: 10px;
        padding: 5px;
    }

    .dx-datagrid .dx-row {
        border-bottom: 1px solid #ccc; /* Border between rows */
    }

    .context-menu {
        display: none;
        position: absolute;
        background-color: white;
        border: 1px solid #ccc;
        box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
        z-index: 1000;
        border-radius: 5px;
    }

    .context-menu-item {
        padding: 10px 15px;
        cursor: pointer;
        font-size: 14px;
        display: flex;
        align-items: center;
    }

        .context-menu-item i {
            margin-right: 10px; /* Space for the icon */
        }

        .context-menu-item:hover {
            background-color: #f1f1f1;
        }

    .popup-message {
        position: fixed;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background-color: #4CAF50; /* Success green color */
        color: white;
        padding: 20px 30px;
        border-radius: 10px;
        font-size: 16px;
        text-align: center;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        z-index: 1001;
    }

    #dataGrid .dx-header-row .dx-datagrid-text-content {
        font-weight: bold;
        color: black;
    }

</style>

<input id="formId" value="uc20101ChartOfAccounts" type="hidden" />
    <div class="title">
        <div class="header-container">
            <div id="ChartOfAccounts" class="no-border-title"></div>
        </div>
    </div>
    <div>
        @await Html.PartialAsync("chartofaccountsFooter")
    </div>
<div id="popupContainer"></div>

<div class="dx-field ledgerResult">
        @(
                Html.DevExtreme().DataGrid<QD.ERP.Web.DAL.Entities.Qry20107ChartOfAccount>()
                .ID("dataGrid")
                .DataSource(d => d.WebApi()
                .Controller("ChartOfAccounts")
                .LoadAction("GetChartOfAccounts")
                .LoadMode(DataSourceLoadMode.Raw)
                .Key("AccountId"))
                .RemoteOperations(false)
                .ShowBorders(true)
                .Columns(columns =>
                {
                    columns.AddFor(m => m.AccountId)
                .Caption(new JS("formatMessage('Account ID')"))
                .Alignment(HorizontalAlignment.Center)
                .Visible(true)
                .CellTemplate(@<text>
            <a href="javascript:void(0);" onclick="openPopup('<%-data.AccountId%>')" style="color: #0398fc; text-decoration: underline;">
                <%-data.AccountId%>
            </a>
        </text>);
                    columns.AddFor(m => m.AccountHead).Caption(new JS("formatMessage('Account Head')")).Alignment(HorizontalAlignment.Center).Visible(true).AllowEditing(true);
                    columns.AddFor(m => m.MasterGroup).Caption(new JS("formatMessage('Account Master')")).Alignment(HorizontalAlignment.Center).GroupIndex(0);
                    columns.AddFor(m => m.AccountHeadArabic).Caption(new JS("formatMessage('Account Head (Ar)')")).Alignment(HorizontalAlignment.Right).Visible(true).AllowEditing(true);
                    columns.AddFor(m => m.AccountGroup).Caption(new JS("formatMessage('Account Group')")).Alignment(HorizontalAlignment.Center).GroupIndex(1);
                    columns.AddFor(m => m.AccountGroup).Caption(new JS("formatMessage('Account Group')")).Alignment(HorizontalAlignment.Center).AllowEditing(true);
                    columns.AddFor(m => m.OpeningBalance).Caption(new JS("formatMessage('Opening Balance')")).Alignment(HorizontalAlignment.Right).Visible(true).AllowEditing(true).CustomizeText(@"
        function(e) {
        if (e.value === null || e.value === undefined) {
        return ''; // Return empty string for null values
        }
        let positiveValue = Math.abs(e.value); // Ensure positive values
        return positiveValue.toLocaleString(undefined, {
        minimumFractionDigits: 2,
        maximumFractionDigits: 2
        });
        }");
                    columns.AddFor(m => m.OpeningBalanceDrCr).Caption(new JS("formatMessage('Dr/Cr')")).Alignment(HorizontalAlignment.Center).Visible(true).AllowEditing(true);
                    columns.AddFor(m => m.IsLedgerObselete).Caption(new JS("formatMessage('Is Ledger Obsolete')")).Alignment(HorizontalAlignment.Center).AllowEditing(true)
                .CellTemplate(@<text>
            <div class="dx-icon">
                <%= data.IsLedgerObselete ? '<i style="color:green" class="fa fa-toggle-on"></i>' : '<i style="color:red" class="fa fa-toggle-off"></i>' %>
            </div>
        </text>)
            .Visible(true);
                columns.AddFor(m => m.AccountGroupId)
    .Caption(new JS("formatMessage('Account Group ID')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.MasterGroupId)
    .Caption(new JS("formatMessage('Master Group ID')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.IsRestrictedEntry)
    .Caption(new JS("formatMessage('Is Restricted Entry')"))
    .Alignment(HorizontalAlignment.Center)
    .Visible(false);

                columns.AddFor(m => m.ReferenceNo)
    .Caption(new JS("formatMessage('Reference No')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.BankAccountNo)
    .Caption(new JS("formatMessage('Bank Account No')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.BankIban)
    .Caption(new JS("formatMessage('Bank IBAN')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.BankName)
    .Caption(new JS("formatMessage('Bank Name')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.BankBranch)
    .Caption(new JS("formatMessage('Bank Branch')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.BankSwiftCode)
    .Caption(new JS("formatMessage('Bank Swift Code')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.BankAccountName)
    .Caption(new JS("formatMessage('Bank Account Name')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.BillingName)
    .Caption(new JS("formatMessage('Billing Name')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.BillingAddress)
    .Caption(new JS("formatMessage('Billing Address')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.BillingContactPerson)
    .Caption(new JS("formatMessage('Billing Contact Person')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.BillingPhoneNo)
    .Caption(new JS("formatMessage('Billing Phone No')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.BillingFaxNo)
    .Caption(new JS("formatMessage('Billing Fax No')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.BillingContactPersonTitle)
    .Caption(new JS("formatMessage('Billing Contact Person Title')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.BillingBankAccount)
    .Caption(new JS("formatMessage('Billing Bank Account')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.LedgerRemarks)
    .Caption(new JS("formatMessage('Ledger Remarks')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.AccountGroupAr)
    .Caption(new JS("formatMessage('Account Group Arabic')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.MasterGroupAr)
    .Caption(new JS("formatMessage('Master Group Arabic')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.MasterOrderNo)
    .Caption(new JS("formatMessage('Master Order No')"))
    .Alignment(HorizontalAlignment.Right)
    .Visible(false);

                columns.AddFor(m => m.MasterGroupCategory)
    .Caption(new JS("formatMessage('Master Group Category')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ChartOfAccountsOrder)
    .Caption(new JS("formatMessage('Chart of Accounts Order')"))
    .Alignment(HorizontalAlignment.Right)
    .Visible(false);

                columns.AddFor(m => m.AccountGroupOrderNo)
    .Caption(new JS("formatMessage('Account Group Order No')"))
    .Alignment(HorizontalAlignment.Right)
    .Visible(false);

                columns.AddFor(m => m.MasterGroupCategoryAr)
    .Caption(new JS("formatMessage('Master Group Category Arabic')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.VoucherAbbr)
    .Caption(new JS("formatMessage('Voucher Abbreviation')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.IsMaintainBillByBill)
    .Caption(new JS("formatMessage('Is Maintain Bill By Bill')"))
    .Alignment(HorizontalAlignment.Center)
    .Visible(false);

                columns.AddFor(m => m.RecordCreatedBy)
    .Caption(new JS("formatMessage('Record Created By')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.RecordCreatedOn)
    .Caption(new JS("formatMessage('Record Created On')"))
    .Alignment(HorizontalAlignment.Center)
    .Visible(false);

                columns.AddFor(m => m.RecordModifiedBy)
    .Caption(new JS("formatMessage('Record Modified By')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.RecordModifiedOn)
    .Caption(new JS("formatMessage('Record Modified On')"))
    .Alignment(HorizontalAlignment.Center)
    .Visible(false);

                columns.AddFor(m => m.NoOfDaysCreditPeriod)
    .Caption(new JS("formatMessage('No Of Days Credit Period')"))
    .Alignment(HorizontalAlignment.Right)
    .Visible(false);

                columns.AddFor(m => m.AccountsContactName)
    .Caption(new JS("formatMessage('Accounts Contact Name')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.AccountsContactMobile)
    .Caption(new JS("formatMessage('Accounts Contact Mobile')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.AccountsContactTitle)
    .Caption(new JS("formatMessage('Accounts Contact Title')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.IsEmployeeAllocated)
    .Caption(new JS("formatMessage('Is Employee Allocated')"))
    .Alignment(HorizontalAlignment.Center)
    .Visible(false);

                columns.AddFor(m => m.ClientVatno)
    .Caption(new JS("formatMessage('Client VAT No')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.SupplierName)
    .Caption(new JS("formatMessage('Supplier Name')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.SupplierNameAr)
    .Caption(new JS("formatMessage('Supplier Name Arabic')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.SupplierAddress)
    .Caption(new JS("formatMessage('Supplier Address')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.SupplierAddressAr)
    .Caption(new JS("formatMessage('Supplier Address Arabic')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.SupplierVatno)
    .Caption(new JS("formatMessage('Supplier VAT No')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientVendorNo)
    .Caption(new JS("formatMessage('Client Vendor No')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.IsPropertyAllocated)
    .Caption(new JS("formatMessage('Is Property Allocated')"))
    .Alignment(HorizontalAlignment.Center)
    .Visible(false);

                columns.AddFor(m => m.AccountSubGroup)
    .Caption(new JS("formatMessage('Account Sub Group')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientGroupVatnumber)
    .Caption(new JS("formatMessage('Client Group VAT Number')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientOtherId)
    .Caption(new JS("formatMessage('Client Other ID')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientOtherIdtype)
    .Caption(new JS("formatMessage('Client Other ID Type')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientAddressStreet)
    .Caption(new JS("formatMessage('Client Address Street')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientAddressStreetAr)
    .Caption(new JS("formatMessage('Client Address Street Arabic')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientAdditionalStreet)
    .Caption(new JS("formatMessage('Client Additional Street')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientAdditionalStreetAr)
    .Caption(new JS("formatMessage('Client Additional Street Arabic')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientBuildingNumber)
    .Caption(new JS("formatMessage('Client Building Number')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientAdditionalNumber)
    .Caption(new JS("formatMessage('Client Additional Number')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientCity)
    .Caption(new JS("formatMessage('Client City')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientCityAr)
    .Caption(new JS("formatMessage('Client City Arabic')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientPostalCode)
    .Caption(new JS("formatMessage('Client Postal Code')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientProvince)
    .Caption(new JS("formatMessage('Client Province')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientProvinceAr)
    .Caption(new JS("formatMessage('Client Province Arabic')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientNeighborhood)
    .Caption(new JS("formatMessage('Client Neighborhood')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientNeighborhoodAr)
    .Caption(new JS("formatMessage('Client Neighborhood Arabic')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.ClientCountryCode)
    .Caption(new JS("formatMessage('Client Country Code')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.SupplierGroupVatnumber)
    .Caption(new JS("formatMessage('Supplier Group VAT Number')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.SupplierOtherIdtype)
    .Caption(new JS("formatMessage('Supplier Other ID Type')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.SupplierOtherId)
    .Caption(new JS("formatMessage('Supplier Other ID')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.SupplierAddressStreet)
    .Caption(new JS("formatMessage('Supplier Address Street')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);

                columns.AddFor(m => m.SupplierAddressStreetAr)
    .Caption(new JS("formatMessage('Supplier Address Street Arabic')"))
    .Alignment(HorizontalAlignment.Left)
    .Visible(false);


                })
             .Editing(editing => editing
             .Mode(GridEditMode.Cell) // ✅ Ensure inline cell editing is enabled
             .AllowUpdating(true) // ✅ Enable editing
             )
            .Height(500)
            .Paging(p => p.PageSize(10))
            .ColumnAutoWidth(true)
            .AllowColumnReordering(true)
            .AllowColumnResizing(true)
            .ColumnFixing(fixing => fixing.Enabled(true))
            .FilterRow(filterRow => filterRow.Visible(false))
            .GroupPanel(groupPanel => groupPanel.Visible(false))
            .ColumnChooser(c => c.Enabled(false).Title(new JS("formatMessage('Show/Hide Column')")).Mode(GridColumnChooserMode.Select))
            @* .HeaderFilter(hf => hf.Visible(true)) *@
            .OnCellPrepared("onCellPrepared")
            )
    </div>
    
</div>


<!-- Context Menu (hidden initially) -->
<div id="contextMenu" class="context-menu">
    <div class="context-menu-item" onclick="editLedger()">
        <i class="fa fa-edit"></i><div id="EditLedgerAccount" ></div>
    </div>
    <div class="context-menu-item" onclick="viewTransactions()">
        <i class="fa fa-search"></i><div id="ViewLedgerTransactions"></div>
    </div>
    <div id="EditLedgerAccount" ></div>
    <div class="context-menu-item" onclick="deleteLedger()">
        <i class="fa fa-trash"></i><div id="DeleteLedgerAccount"></div> 
    </div>
</div>

<script type="text/javascript">
    $(function () {
        $("#EditLedgerAccount").dxTextBox({
            text: formatMessage("Edit Ledger Account"),
            readOnly: true

        });
    });
    $(function () {
        $("#ViewLedgerTransactions").dxTextBox({
            text: formatMessage("View Ledger Transactions"),
            readOnly: true

        });
    });
    $(function () {
        $("#DeleteLedgerAccount").dxTextBox({
            text: formatMessage("Delete Ledger Account"),
            readOnly: true

        });
    });
    $(function () {
        $("#ChartOfAccounts").dxTextBox({
            text: formatMessage("Chart Of Accounts"),
            readOnly: true

        });
    });
    function openPopup(accountId) {
        $("#popupContainer").dxPopup({
            contentTemplate: function (contentElement) {
                $("<iframe>")
                    .attr("src", "/pulse/Finance/LedgerAccount?AccountId=" + accountId)
                    .css({ width: "100%", height: "600px", border: "none" })
                    .appendTo(contentElement);
            },
            width: 1200,
            height: 600,
            title: "Ledger Account Details",
            showCloseButton: true,  // ✅ Displays the (X) close button
            visible: true,
            closeOnOutsideClick: true,  // ✅ Allows closing when clicking outside the popup
            onHiding: function () {
                $("#popupContainer").dxPopup("instance").dispose(); // ✅ Cleanup popup instance
                location.reload();  // ✅ Reloads the page after closing popup
            }
        }).dxPopup("instance").show();
    }



    $(function () {
        var contextMenu = $("#contextMenu");

        // Hide context menu when clicking outside
        $(document).on("click", function () {
            contextMenu.hide();
        });

        // Right-click event to show context menu
        $("#dataGrid").on("contextmenu", ".dx-data-row", function (e) {
            e.preventDefault();

            // Get instance of the DataGrid
            var dataGridInstance = $("#dataGrid").dxDataGrid("instance");
            var visibleRows = dataGridInstance.getVisibleRows(); // Retrieve visible rows
            var rowIndex = $(this).index();

            // Debugging: Ensure rowIndex is correct
            console.log("Row Index:", rowIndex);

            var rowData = visibleRows[rowIndex]?.data; // Retrieve the row data from visible rows
            console.log("Row Data:", rowData); // Debugging: Log the row data

            if (rowData && rowData.AccountId) {
                window.selectedRowData = rowData; // Store selected row data for use in actions
                contextMenu.css({ top: e.pageY + "px", left: e.pageX + "px" }).show();
            } else {
                alert("Error: AccountId is undefined. Please select a valid row.");
            }
        });
    });
    // Function to handle Edit Ledger Account action
    function editLedger() {
        var rowData = window.selectedRowData;
        if (rowData && rowData.AccountId) {   
            // Navigate to frm201LedgerAccount with Edit mode
           // window.location.href = `/frm201LedgerAccount?mode=edit&accountId=${rowData.AccountId}`;
            window.location.href = `/pulse/Finance/LedgerAccount?AccountId=${selectedRowData.AccountId}&IsDisabled=${encodeURIComponent(selectedRowData.IsDisabled)}`;
        } else {
            alert("No valid row selected for editing.");
        }
    }

    // Function to handle View Ledger Transactions action
    function viewTransactions() {
        var rowData = window.selectedRowData;
        if (rowData && rowData.AccountId) {
            // Navigate to frm201LedgerAccount with View Transactions mode
            window.location.href = `/pulse/Finance/AccountingLedgers?AccountId=${selectedRowData.AccountId}&IsDisabled=${encodeURIComponent(selectedRowData.IsDisabled)}`;

           // window.location.href = `/frm201LedgerAccount?mode=view&accountId=${rowData.AccountId}`;
        } else {
            alert("No valid row selected to view transactions.");
        }
    }

    $("#dataGrid").on("contextmenu", ".dx-data-row", function (e) {
        e.preventDefault();
        var rowIndex = $(this).index();
        var rowData = $("#dataGrid").dxDataGrid("instance").getDataSource().items()[rowIndex];
        window.selectedRowData = rowData;
    });

    function deleteLedger() {
        var rowData = window.selectedRowData;
        if (rowData && rowData.AccountId) {
            // Correct API endpoint URL
            var deleteUrl = `/api/ChartOfAccounts/Delete?accountId=${rowData.AccountId}`;

            console.log("Delete URL:", deleteUrl);

            // Confirmation dialog
            if (confirm("You are about to delete the Ledger Account from the database")) {
                $.ajax({
                    url: deleteUrl,
                    type: 'POST', // Ensure the correct HTTP method
                    success: function (response) {
                        if (response.success) {
                            alert(response.message || "Ledger Account deleted successfully.");
                            $("#dataGrid").dxDataGrid("instance").refresh(); // Refresh the grid
                        } else {
                            alert(response.message || "Error deleting ledger account.");
                        }
                    },
                    error: function (xhr, status, error) {
                        console.error("Error deleting ledger account:", error);
                        var errorMessage = xhr.responseJSON?.message || "An unexpected error occurred.";
                        alert(errorMessage);
                    }
                });
            }
        } else {
            alert("Error: AccountId is undefined. Please select a valid row.");
        }
    }


    function onCellPrepared(e) {
        if (e.rowType === "data" && e.column.dataField === "AccountHead") {
            if (!e.data.IsLedgerObselete) {
                e.cellElement.css({
                    "background-color": "red",
                    "color": "white",
                    "background-color": "white",
                    "color": "#ef5353",
                    "font-weight":"bold"
                });
            }else{
                e.cellElement.css({
                    "background-color": "white",
                    "color": "green",
                    "font-weight":"bold"
                });
            }
        }
    }
</script>
