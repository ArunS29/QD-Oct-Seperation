@page
@using QD.ERP.Web.DAL.Entities
@{

}
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">

<style type="text/css">
    .cards-container-title {
        text-align: left;
        font-size: 2rem;
        margin-bottom: 10px;
        color: white;
       /*  background-color: black; */
        padding: 5px;

    }

    .card {
        border: 1px solid #ddd;
        border-radius: 2px;
        /* box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1); */
        background-color: black;
         color: white;
        width: 100%;
        padding: 0px;
        box-sizing: border-box;
    }

    /* Override Bootstrap padding/margin if necessary */
    .container {
       /*  max-width: 65%; */
         width: 100%;
        padding: 20px;
        margin: 0 auto;
        background-color: #595959;
    }
     .horizontal-container {
        display: flex;
        gap: 5px; /* Adds spacing between fields */
        justify-content: start;
        align-items: center;
        margin: 5px 0;
    }

</style>
<h2 class="cards-container-title">Employee Salary Payables</h2>

<div class="container my-3">
    <div class="row g-3">
        <div class="col-12">
            <div class="card p-3 shadow-sm bg-blue text-danger" style="background-color: #B4E1F5;">

                <!-- Row for Voucher Entry ID, Payment Account, Amount, Dr/Cr, Salary Paid -->
                <div class="row mb-3">
                    <!-- Voucher Entry ID -->
                    <div class="col-md-2">
                        <label for="PurchaseVoucherNo" class="form-label">Voucher Entry ID:</label>
                        @(Html.DevExtreme().TextBox()
                                .ID("VoucherEntryID")
                                .Value(null)
                                .Disabled(true)
                                .Width("100%"))
                    </div>

                    <!-- Payment Account -->
                    <div class="col-md-3">
                        <label for="" class="form-label">Payment Account:</label>
                        @(Html.DevExtreme().DropDownBox()
                                .DataSource(d => d.WebApi()
                                    .Controller("LedgerAccoutMaster")
                                    .LoadAction("GetPaymentAccounts")
                                    .LoadMode(DataSourceLoadMode.Raw)
                                    .Key("AccountId"))
                                .LabelMode(EditorLabelMode.Outside)
                                .ID("accountDropDownBox")
                                .ShowClearButton(true)
                                .AcceptCustomValue(true)
                                .OpenOnFieldClick(false)
                                .ValueChangeEvent("input")
                                .HoverStateEnabled(true)
                                .ValueExpr("AccountId")
                                .DisplayExpr("AccountHead")
                                .Width("100%")
                                .Disabled(true)
                                .ContentTemplate(new TemplateName("PaymentAmountContentTemplate")))
                    </div>

                    <!-- Add Button 1 -->
                    <div class="col-md-1 d-flex align-items-end">
                        @(Html.DevExtreme().Button()
                                .ID("PayAddAHead")
                                .Icon("plus")
                                .Width("100%")
                                     .Disabled(true))
                    </div>

                    <!-- Amount -->
                    <div class="col-md-2">
                        <label for="Amount" class="form-label">Amount:</label>
                        @(Html.DevExtreme().NumberBox()
                                .ID("txtVoucherAmount")
                                .Value(null)
                                .ValidationRules(rules =>
                                {
                                    rules.AddRequired().Message("Voucher Amount is required");
                                    rules.AddRange().Min(0).Message("Voucher Amount cannot be negative");
                                })
                                .Placeholder("0.000")
                                .Format("#,##0.000")
                                .InputAttr(new { style = "text-align: right;" })
                                .Min(0)
                                .Width("100%")
                                     .Disabled(true))
                    </div>

                    <!-- Dr/Cr -->
                    <div class="col-md-2">
                        <label for="DrCr" class="form-label">Dr/Cr:</label>
                        @(Html.DevExtreme().SelectBox()
                                .ID("sbDrCr")
                                .DataSource(new[] { "Dr", "Cr" })
                                .Value("Dr")
                                .Placeholder("Select Transaction Type")
                                .Width("100%"))
                    </div>

                    <!-- Salary Paid -->
                    <div class="col-md-2">
                        <label for="SalaryPaid" class="form-label">Reference:</label>
                        @(Html.DevExtreme().SelectBox()
                                .ID("SalaryPaid")
                                .DataSource(new[] { "Salary Paid", "Salary Payable" })
                                .Value("Salary Paid")
                                .Placeholder("Select Reference Type")
                                .Width("100%"))
                    </div>
                </div>
            </div>
        </div>

        <!-- Card -->
        <div class="col-12">
            <div class="card p-3 shadow-sm">
                <!-- Row for Employee, Buttons, Payable, Amount, Add Button -->
                <div class="row mb-3 align-items-end">
                    <!-- Employee Dropdown -->
                    <div class="col-md-3">
                        <label for="employeeDropdown" class="form-label">Select the Employee:</label>
                        @(Html.DevExtreme().DropDownBox()
                                .DataSource(d => d.WebApi()
                                    .Controller("LedgerAccountMaster")
                                    .LoadAction("GetEmployeeDetails")
                                    .LoadMode(DataSourceLoadMode.Raw)
                                    .Key("EmployeeNo"))
                                          .ValidationRules(rules => {   
                                              rules.AddRequired()
                                             .Message("Employee details is required");
                                                                            })
                                .LabelMode(EditorLabelMode.Outside)
                                .ID("accountEmployeeDropDownBox")
                                .ShowClearButton(true)
                                .AcceptCustomValue(true)
                                .Width("100%")
                                     .OpenOnFieldClick(false)
                                            .ValueChangeEvent("input")
                                            .HoverStateEnabled(true)
                                                    .ValueExpr("EmployeeNo")
                                                    .DisplayExpr("EmployeeName")
                                            .OnInput("dropDownBoxOnInput")
                                                .OnOpened("onOpened")
                                                .OnKeyDown("onKeyDown")
                                                    .TabIndex(1)
                                                .DropDownOptions(o => o
                                                .Width(1000)
                                                )
                                                    //.OnValueChanged("dropDownBoxValueChanged")
                                                        .ContentTemplate(new TemplateName("EmployeeContentTemplate")))
                    </div>

                    <!-- Add Button 1 -->
                    <div class="col-md-1 d-flex align-items-end">
                        @(Html.DevExtreme().Button()
                                .ID("btnAddAHead")
                                .Icon("plus")
                                .Width("100%"))
                    </div>

                    <!-- Against Payable For -->
                    <div class="col-md-3">
                        <label for="payableDropdown" class="form-label">Against Payable For:</label>
                        @(Html.DevExtreme().DropDownBox()
                                .DataSource(d => d.WebApi()
                                    .Controller("LedgerAccountMaster")
                                    .LoadAction("GetAgainstPayable")
                                    .LoadMode(DataSourceLoadMode.Raw)
                                    .Key("EmployeeNo"))
                                        .ValidationRules(rules =>
                                                                  {
                                                                        rules.AddRequired()
                                                                        .Message("Against Payable is required");
                                                                    })
                                .LabelMode(EditorLabelMode.Outside)
                                .ID("againstDropDownBoxPayable")
                                .ShowClearButton(true)
                                .AcceptCustomValue(true)
                                    .OpenOnFieldClick(false)
                                                 .ValueChangeEvent("input")
                                                 .HoverStateEnabled(true)
                                                    .ValueExpr("EmployeeNo")
                                                      .DisplayExpr("ReferenceNo")
                                                    .OnInput("dropDownBoxOnInput")
                                                    .OnOpened("onOpened")
                                .Width("100%")
                                      .OnKeyDown("onKeyDown")
                                                    .TabIndex(1)
                                                .DropDownOptions(o => o
                                                .Width(1000)
                                                )
                                          .ContentTemplate(new TemplateName("AgainstPayableContentTemplate")))
                    </div>

                    <!-- Add Button 2 -->
                    <div class="col-md-1 d-flex align-items-end">
                        @(Html.DevExtreme().Button()
                                .ID("btnAddAHead1")
                                .Icon("plus")
                                .Width("100%"))
                    </div>

                    <!-- Amount Field -->
                       <div class="col-md-2">
            <label for="Amount" class="button-alignment-down">Amount</label>
                        @(
                                                    Html.DevExtreme().NumberBox()
                                                    .ID("txtVoucherAmount1")
                                                    .Value(null)
                                                    .ValidationRules(rules =>
                                                    {
                                                        rules.AddRequired()
                                                        .Message("Voucher Amount is required");
                                                        rules.AddRange().Min(0).Message("Voucher Amount cannot be negative");
                                                    })
                                                    .Placeholder("0.000")
                                                         .Format("###,###,###,##0.00")  // Numeric format with thousands separator and two decimal places
                                                .InputAttr(new { type = "number", step = "0.01", style = "font-family: Cambria; font-size: 11pt; font-weight: bold; color: maroon; text-align: right;", maxLength = 16, autocomplete = "off" })
                                                             .Width("100%")// HTML attribute to allow decimal input
                                                    )

        </div>
                  
                        

                    <!-- Add Voucher Entry Button -->
                    <div class="col-md-2 d-flex align-items-end">
                        @(Html.DevExtreme().Button()
                                .ID("btnVoucherEntry")
                                .Text("ADD")
                                .Icon("plus")
                                .Width("100%")
                                     .OnClick("function() { addVoucherEntry(); }"))
                    </div>
                </div>
 <!-- DataGrid -->
                <div>
                    @(Html.DevExtreme().DataGrid<QD.ERP.Web.Areas.Finance.Models.CombinedEntityViewModel>()
       // PaymentForm.DAL.Entities.Tbl20114SalaryPayableMaster
                            .ID("dataGrid")
                            .RemoteOperations(false)
                            .Columns(columns =>
                            {
                                columns.AddFor(m => m.SalaryPayableLedgerNo).Caption("Salary Payable Ledger").Alignment(HorizontalAlignment.Center);
                                columns.AddFor(m => m.ReferenceType).Caption("Reference Type").Alignment(HorizontalAlignment.Center);
                                columns.AddFor(m => m.ReferenceNo).Caption("Reference No").Alignment(HorizontalAlignment.Center);
                                columns.AddFor(m => m.EmployeeNo).Caption("Employee ID").Alignment(HorizontalAlignment.Center);
                                columns.AddFor(m => m.EmployeeName).Caption("Employee Name").Alignment(HorizontalAlignment.Center);
                                columns.AddFor(m => m.Amount).Caption("Amount");
                                    columns.AddFor(m => m.DrCr).Caption("Dr/Cr").Alignment(HorizontalAlignment.Center);
                            })
                            .AllowColumnReordering(true)
                            .AllowColumnResizing(true)
                            .ColumnFixing(fixing => fixing.Enabled(true))
                            .HeaderFilter(filter => filter.Visible(true))
                            .Height(250)
                            .ColumnAutoWidth(true)
                            .FilterRow(filterRow => filterRow.Visible(true))
                            //.FilterBuilderPopup(popup => popup.Position(pos => pos.Of("#dataGrid").At("left top").My("left top")))
                            .GroupPanel(groupPanel => groupPanel.Visible(false))
                            .ShowBorders(true)
                            .Width("100%"))
                </div>
            </div>

            <!-- Action Buttons -->
            <div class="mt-3 d-flex justify-content-between">
                @(Html.DevExtreme().Button()
                        .ID("btnPreview")
                        .Text("Preview")
                        .Icon("fullscreen"))

                @(Html.DevExtreme().Button()
                        .ID("btnBack")
                        .Text("Back")
                        .Icon("arrowleft"))
            </div>
        </div>
        @using (Html.DevExtreme().NamedTemplate("PaymentAmountContentTemplate"))
        {
            @(Html.DevExtreme().DataGrid()

                                    .ID("accountpaymentamount-datagrid")
                                    .DataSource(d => d.WebApi()
                                    .Controller("LedgerAccountMaster")
                                            .LoadAction("GetPaymentAccounts")
                                    .LoadMode(DataSourceLoadMode.Raw)
                                    .Key("AccountId")
                                    )
                                    .WordWrapEnabled(true)
                                    .ShowBorders(true)
                                    .SearchPanel(sp => sp.HighlightSearchText(true))
                                    .Selection(sl => sl.Mode(SelectionMode.Single)) // Set to Single selection mode
                                    .OnSelectionChanged("dataGridSelectionChanged")
                                    .OnInitialized("dataGridInitialized")
                                    .HeaderFilter(p => p.Visible(true))
                                    .ShowBorders(true)
                                    .HoverStateEnabled(true)
                                    .Paging(p => p.PageSize(15))
                                    .FilterRow(f => f.Visible(true))
                                    .Scrolling(s => s.Mode(GridScrollingMode.Virtual))
                                    .Height(345)
                                    .Selection(s => s.Mode(SelectionMode.Single))
                                    .SelectedRowKeys(new JS(@"component.option(""value"") ? [component.option(""value"")] : []"))
                                     .Columns(columns =>
                        {
                            @*       columns.Add().DataField("MasterGroupId");
                            columns.Add().DataField("MasterGroup");
                            columns.Add().DataField("AccountGroupId"); *@
                            columns.Add().DataField("AccountId");
                            columns.Add().DataField("AccountHead");
                            columns.Add().DataField("AccountHeadArabic");
                            columns.Add().DataField("ReferenceNo");
                            columns.Add().DataField("IsLedgerObselete").Alignment(HorizontalAlignment.Center)
                        .CellTemplate(@<text>
        <div class="dx-icon">
        <%= data.IsLedgerObselete ? '<i style="color:green" class="fa fa-toggle-on"></i>' : '<i style="color:red" class="fa fa-toggle-off"></i>' %>
        </div>
        </text>);

                        })
                                )
        }
        @using (Html.DevExtreme().NamedTemplate("EmployeeContentTemplate"))
        {
            @(Html.DevExtreme().DataGrid()

                                        .ID("accountpaymentamount-datagrid")
                                        .DataSource(d => d.WebApi()
                                        .Controller("LedgerAccountMaster")
                                        .LoadAction("GetEmployeeDetails")
                                        .LoadMode(DataSourceLoadMode.Raw)
                                            .Key("EmployeeNo")

                                        )
                                        .WordWrapEnabled(true)
                                        .ShowBorders(true)
                                        .SearchPanel(sp => sp.HighlightSearchText(true))
                                        .Selection(sl => sl.Mode(SelectionMode.Single)) // Set to Single selection mode
                                                .OnSelectionChanged("dataGridEmployeeSelectionChanged")
                                        .OnInitialized("dataGridInitialized")
                                        .HeaderFilter(p => p.Visible(true))
                                        .ShowBorders(true)
                                        .HoverStateEnabled(true)
                                        .Paging(p => p.PageSize(15))
                                        .FilterRow(f => f.Visible(true))
                                        .Scrolling(s => s.Mode(GridScrollingMode.Virtual))
                                        .Height(345)
                                        .Selection(s => s.Mode(SelectionMode.Single))
                                        .SelectedRowKeys(new JS(@"component.option(""value"") ? [component.option(""value"")] : []"))
                                         .Columns(columns =>
                            {
                                columns.Add().DataField("EmployeeNo");
                                columns.Add().DataField("EmployeeName");
                                columns.Add().DataField("NationalID");


                            })
                                    )
        }

        @using (Html.DevExtreme().NamedTemplate("AgainstPayableContentTemplate"))
        {
            @(Html.DevExtreme().DataGrid()

                                            .ID("accountpayable-datagrid")
                                            .DataSource(d => d.WebApi()
                                            .Controller("LedgerAccountMaster")
                                            .LoadAction("GetAgainstPayable")
                                                 .LoadParams(new { inputParameter = new JS("employeeval") })
                                            .LoadMode(DataSourceLoadMode.Raw)
                                                .Key("ReferenceNo")

                                            )
                                            .WordWrapEnabled(true)
                                            .ShowBorders(true)
                                            .SearchPanel(sp => sp.HighlightSearchText(true))
                                            .Selection(sl => sl.Mode(SelectionMode.Single)) // Set to Single selection mode
                                                    .OnSelectionChanged("dataGridAgainstPayableSelectionChanged")
                                            .OnInitialized("dataGridInitialized")
                                            .HeaderFilter(p => p.Visible(true))
                                            .ShowBorders(true)
                                            .HoverStateEnabled(true)
                                            .Paging(p => p.PageSize(15))
                                            .FilterRow(f => f.Visible(true))
                                            .Scrolling(s => s.Mode(GridScrollingMode.Virtual))
                                            .Height(345)
                                            .Selection(s => s.Mode(SelectionMode.Single))
                                            .SelectedRowKeys(new JS(@"component.option(""value"") ? [component.option(""value"")] : []"))
                                             .Columns(columns =>
                                {
                                    columns.Add().DataField("EmployeeNo");
                                    columns.Add().DataField("EmployeeName");
                                    columns.Add().DataField("ReferenceNo");
                                    columns.Add().DataField("PayableAmount");
                                    columns.Add().DataField("Paid");
                                    columns.Add().DataField("Balance");


                                })
                                        )
        }
    </div>
</div>

<script>
    // var globalVariable = "SomeValue";
    var mARkey = null;
    let mARresponse = null;
    let employeeval = null;
    let accheadid = null;

     $(document).ready(function () {
    // Retrieve VoucherNo from query parameters
    const urlParams = new URLSearchParams(window.location.search);
    const voucherNo = urlParams.get('VoucherNo');
    const paymentamount = urlParams.get('PaymentAmount');
    const amount = urlParams.get('Amount');
         const aid = urlParams.get('AccountHeadID');
         accheadid = aid;

    if (voucherNo) {
        // Wait for DevExtreme TextBox to initialize
        const interval = setInterval(() => {
            const voucherEntryIDInstance = $("#VoucherEntryID").dxTextBox("instance");
             const voucheramountIDInstance = $("#txtVoucherAmount").dxNumberBox("instance");
            if (voucherEntryIDInstance) {
                // Set the value and stop the interval
                voucherEntryIDInstance.option("value", voucherNo);
                 dataGridSelectionChanged({ selectedRowKeys: [paymentamount] });
                  voucheramountIDInstance.option("value", amount);
                clearInterval(interval);
            }
        }, 100); // Retry every 100ms
    }
});


    $("#AMbtnSave").on("click", function (e) {
        e.preventDefault();

        // debugger;
        var VM = {
            // alert($("#VoucherNo").dxTextBox("instance").option("value"));
            "AccountGroupId": $("#AccountGroupID").dxTextBox("instance").option("value"),

            "AccountGroup": $("#AccountGroup").dxTextBox("instance").option("value"),

            "AccountMaster": $("#accountMasterDropDownBox").dxDropDownBox("instance").option("value"),

            "AccountGroupIDAr": $("#AccountGroupID1").dxTextBox("instance").option("value"),
            "AccountGroupAr": $("#AccountGroup1").dxTextBox("instance").option("value"),
            "AccountMasterAr": $("#accountDropDownBoxArabic").dxDropDownBox("instance").option("value"),
            "AccountGroupUnder": mARresponse

        };

        $.ajax({
            type: "POST",
            url: "/api/LedgerAccountMaster/SaveLedgerMasterDetails",
            contentType: "application/json",
            data: JSON.stringify(VM),
            dataType: "json",
            success: function (response) {
                if (response.success) {
                    alert("Data saved successfully..");
                 //   window.location.href = "http://localhost:5000/";

                } else {
                    alert("Update failed: " + response.message);
                }
            },
            error: function (xhr, status, error) {
                alert("Error occurred while updating data: " + error);
            }
        });
    });

    $("#btnBack").on("click", function (e) {
        e.preventDefault();
        alert("Are you sure want to exit?");
        window.location.href = "http://localhost:5000/";
        // debugger;
    });


        function addCostAllocation() {
            $("#costAllocationPopup").dxPopup("instance").show();
        }
        function saveCostAllocation() {
            $("#costAllocationPopup").dxPopup("instance").close();
        }
        function dropDownBoxOnInput(e) {
            const ddbInstance = e.component;
            if (!ddbInstance.option("opened")) ddbInstance.open();


            const text = ddbInstance.option("text");
            if (dataGrid) {
                dataGrid.option("searchPanel.text", text);
            }
        }

        function onOpened(e) {
            setTimeout(() => {
                e.component.focus();
            });
        }

        function onKeyDown(e) {
            const ddbInstance = e.component;
            if (e.event.keyCode !== 40) return;
            if (!ddbInstance.option("opened")) {
                ddbInstance.open();
            } else {
                let focusedIndex = dataGrid.option("focusedRowIndex");
                let visibleRows = dataGrid.getVisibleRows().length - 1;
                if (focusedIndex === -1 || visibleRows < focusedIndex) focusedIndex = 0;
                dataGrid.focus(dataGrid.getRowElement(focusedIndex));
            }
        }

        //debugger;

        function dataGridSelectionChanged(e) {
            const selectedKey = e.selectedRowKeys[0];
            const ddbInstance = $("#accountDropDownBox").dxDropDownBox("instance");

            if (selectedKey) {
                ddbInstance.option("value", selectedKey);
                if (ddbInstance.option("opened")) {
                    ddbInstance.close();
                }
            }

        }
            function dataGridEmployeeSelectionChanged(e) {
            const selectedKey = e.selectedRowKeys[0];
            const ddbInstance = $("#accountEmployeeDropDownBox").dxDropDownBox("instance");
            employeeval=selectedKey;

            if (selectedKey) {
                ddbInstance.option("value", selectedKey);
                if (ddbInstance.option("opened")) {
                    ddbInstance.close();
                }
            }

        }
          function dataGridAgainstPayableSelectionChanged(e) {
            const selectedKey = e.selectedRowKeys[0];
            const ddbInstance = $("#againstDropDownBoxPayable").dxDropDownBox("instance");
             const payableAmount = e.selectedRowsData[0].PayableAmount;

            if (selectedKey) {

                ddbInstance.option("value", selectedKey);
                if (ddbInstance.option("opened")) {
                    ddbInstance.close();
                }
            const voucherAmountBox = $("#txtVoucherAmount1").dxNumberBox("instance");
            voucherAmountBox.option("value", payableAmount);
            }

        }

        // function dataGridAccountHeadSelectionChanged(e) {
        //     const selectedKey = e.selectedRowKeys[0];
        //     const ddbInstance = $("#AccountHeadDropDown").dxDropDownBox("instance");

        //     if (selectedKey) {
        //         ddbInstance.option("value", selectedKey);
        //         if (ddbInstance.option("opened")) {
        //             ddbInstance.close();
        //         }
        //     }

        // }
        function dataGridInitialized(e) {
            dataGrid = e.component;
        }




    function dataGridAccountMasterSelectionChanged(e) {
            const selectedKey = e.selectedRowKeys[0];
        const ddbInstance = $("#accountMasterDropDownBox").dxDropDownBox("instance");

            if (selectedKey) {
                ddbInstance.option("value", selectedKey);
                if (ddbInstance.option("opened")) {
                    ddbInstance.close();
                }
            }

        var selectedValue = selectedKey;

        $.ajax({

            type: "GET",
            url: `/api/LedgerAccountMaster/GetAccountMasterAR?MasterGroup=${encodeURIComponent(selectedValue)}`, // Pass value as a query parameter
            contentType: "application/json",
            dataType: "json",
            success: function (response) {
                if (response) {

                    mARresponse = response;


                    // Update the second dropdown's DataGrid instance with the data
                   // $("#accountDropDownBoxArabic").dxDataGrid("instance").option("dataSource", [response]); // Ensure response is array
                    //const responseKey = response[0]?.key || response.key; // Adjust based on response structure
                    dataGridAccountMasterSelectionChangedAr({ selectedRowKeys: [mARresponse] });

                } else {
                    alert("No data found for the selected value.");
                }
            },
            error: function (xhr, status, error) {
                console.error("Error occurred while fetching data:", error);
                alert("Error occurred while updating data: " + error);
            },
        });



        }



        function dataGridAccountMasterInitialized(e) {
            dataGrid = e.component;
        }


    function dataGridAccountMasterSelectionChangedAr(e) {
        const selectedKey = e.selectedRowKeys[0];
        const ddbInstance = $("#accountDropDownBoxArabic").dxDropDownBox("instance");

        if (selectedKey) {
           ddbInstance.option("value", selectedKey);
            if (ddbInstance.option("opened")) {
                ddbInstance.close();
            }
        }


    }



    function dataGridAccountMasterInitializedAr(e) {
        dataGrid = e.component;
    }




    async function translateToArabic(text) {
        // try {
        //     const response = await fetch(`https://api.mymemory.translated.net/get?q=${encodeURIComponent(text)}&langpair=en|ar`);
        //     const data = await response.json();
        //     return data.responseData.translatedText;
        // } catch (error) {
        //     console.error("Translation failed:", error);
        //     return "Translation Error";
        // }
        const apiUrl = `https://api.mymemory.translated.net/get?q=${encodeURIComponent(text)}&langpair=en|ar&mt=1`;

        try {
            const response = await fetch(apiUrl);
            const data = await response.json();

            if (data && data.responseData) {
                return data.responseData.translatedText || "No translation found";
            } else if (data.matches && data.matches.length > 0) {
                // Fallback: Check for alternative matches if responseData is empty
                return data.matches[0].translation || "No match found";
            } else {
                return "Translation Error";
            }
        } catch (error) {
            console.error("Translation failed:", error);
            return "Translation Error";
        }
    }


    document.getElementById("convertToArabic").addEventListener("click", async () => {
        const englishText = document.getElementById("AccountGroup").value;

        // if (englishText.trim() === "") {
        //     alert("Please enter text to translate.");
        //     return;
        // }

        // Await the result of the asynchronous translation function
        const arabicTranslation = await translateToArabic(englishText);

        // Update the Arabic text field with the resolved value
        const accountGroup1Instance = $("#AccountGroup1").dxTextBox("instance");
        accountGroup1Instance.option("value", arabicTranslation);
    });

    function onCheckboxInit(e) {
        $(e.element).attr("data-id", e.component.option("id"));
    }

    function syncCard4(e) {
        // Get the instance of the source checkbox (Card 3)
        const sourceCheckbox1 = $("#ChooseifthisgrouptobeunderSales").dxCheckBox("instance");
        const sourceCheckbox2 = $("#ChooseifthisgrouptobeunderPurchases").dxCheckBox("instance");
        const sourceCheckbox3 = $("#ChooseifthisgrouptobeunderSalaryPayable").dxCheckBox("instance");
        const sourceCheckbox4 = $("#ChooseifthisgroupwanttobeReconcilied").dxCheckBox("instance");

        // Get the instance of the target checkbox (Card 4)
        const targetCheckbox1 = $("#ChooseifthisgrouptobeunderSales1").dxCheckBox("instance");
        const targetCheckbox2 = $("#ChooseifthisgrouptobeunderPurchases1").dxCheckBox("instance");
        const targetCheckbox3 = $("#ChooseifthisgrouptobeunderSalaryPayable1").dxCheckBox("instance");
        const targetCheckbox4 = $("#ChooseifthisgroupwanttobeReconcilied1").dxCheckBox("instance");


        // Assign the value of the source checkbox to the target checkbox
        targetCheckbox1.option("value", sourceCheckbox1.option("value"));
        targetCheckbox2.option("value", sourceCheckbox2.option("value"));
        targetCheckbox3.option("value", sourceCheckbox3.option("value"));
        targetCheckbox4.option("value", sourceCheckbox4.option("value"));

    }

    //  $(document).ready(function () {
    //     // Retrieve VoucherNo from query parameters
    //     const urlParams = new URLSearchParams(window.location.search);
    //     const voucherNo = urlParams.get('VoucherNo');
    //     alert(voucherNo);

    //     // Bind VoucherNo to VoucherEntryID TextBox if available
    //     if (voucherNo) {
    //         $("#VoucherEntryID").dxTextBox("instance").option("value", voucherNo);
    //     }
    // });

    //function addVoucherEntry() {
    //     var AccountEmployeeValidator = $("#accountEmployeeDropDownBox").dxValidator("instance");
    //      var AccountDropDownPayableValidator = $("#againstDropDownBoxPayable").dxValidator("instance");
    //        var voucherAmtValidator = $("#txtVoucherAmount1").dxValidator("instance");

    //        var accountemployeeValidation = AccountEmployeeValidator.validate();
    //        var accountpayableValidation = AccountDropDownPayableValidator.validate();
    //        var voucheramountValidation = voucherAmtValidator.validate();

    //        if(accountemployeeValidation.isValid && accountpayableValidation.isValid && voucheramountValidation.isValid)
    //        {
    //            var VE = {
    //                // "SalaryPayableId": $("#VoucherEntryID").dxTextBox("instance").option("value"),
    //                // "VoucherDate": $("#VoucherDate").dxDateBox("instance").option("value"),
    //                // "EffectiveDate": $("#EffectiveDate").dxDateBox("instance").option("value"),
    //               "ReferenceType": $("#SalaryPaid").dxSelectBox("instance").option("value"),
    //                "EmployeeName": $("#accountEmployeeDropDownBox").dxDropDownBox("instance").option("value"),
    //                "ReferenceNo": "01",
    //                "DrCr": $("#sbDrCr").dxSelectBox("instance").option("value"),
    //                "EmployeeNo": "102",
    //                "EmployeeName": "",
    //                "Amount":$("#txtVoucherAmount1").dxNumberBox("instance").option("value"),

    //            };
    //            $.ajax({
    //                type: "POST",
    //                url: "/api/LedgerAccountMaster/AddEmployeeEntry",
    //                contentType: "application/json",
    //                data: JSON.stringify(VE),
    //                dataType: "json",
    //                success: function (response) {
    //                    $("#dataGrid").dxDataGrid("instance").option("dataSource", response.data);
    //                },
    //                error: function (xhr, status, error) {
    //                    alert("Error occurred while updating data: " + error);
    //                }

    //            });
    //            } else {
    //         DevExpress.ui.notify("Please fill required details!", "error", 2000);
    //        }
    //            //$("#costAllocationPopup").dxPopup("instance").show();
    //        }

    function addVoucherEntry() {
        var AccountEmployeeValidator = $("#accountEmployeeDropDownBox").dxValidator("instance");
        var AccountDropDownPayableValidator = $("#againstDropDownBoxPayable").dxValidator("instance");
        var voucherAmtValidator = $("#txtVoucherAmount1").dxValidator("instance");

        var accountemployeeValidation = AccountEmployeeValidator.validate();
        var accountpayableValidation = AccountDropDownPayableValidator.validate();
        var voucheramountValidation = voucherAmtValidator.validate();

        if (accountemployeeValidation.isValid && accountpayableValidation.isValid && voucheramountValidation.isValid) {
           
            var VE = {
                SalaryPayableLedgerNo: accheadid,
                ReferenceType: $("#SalaryPaid").dxSelectBox("instance").option("value"),
               // EmployeeName: null,
                ReferenceNo: null,
                DrCr: $("#sbDrCr").dxSelectBox("instance").option("value"),
                EmployeeNo: null, // Update dynamically if needed
                Amount: $("#txtVoucherAmount1").dxNumberBox("instance").option("value"),
            };
           
            // Get ReferenceNo from selected row
            var selectedRowData = $("#accountpayable-datagrid").dxDataGrid("instance").getSelectedRowsData();
            if (selectedRowData.length > 0) {
                VE.ReferenceNo = selectedRowData[0].ReferenceNo;
                VE.EmployeeNo = selectedRowData[0].EmployeeNo;
                //VE.EmployeeName = selectedRowData[0].EmployeeName;
            } else {
                DevExpress.ui.notify("Please select a row from the grid!", "error", 2000);
                return;
            }
         
            $.ajax({
                type: "POST",
                url: `/api/LedgerAccountMaster/AddEmployeeEntry?EmployeeName=${encodeURIComponent(selectedRowData[0].EmployeeName)}`, // Pass value as a query parameter
               // url: "/api/LedgerAccountMaster/AddEmployeeEntry?MasterGroup=${encodeURIComponent(selectedValue)}`",
                contentType: "application/json",
                data: JSON.stringify(VE),
                dataType: "json",
                success: function (response) {
                    $("#dataGrid").dxDataGrid("instance").option("dataSource", response.data);
                    DevExpress.ui.notify("Data saved successfully!", "success", 2000);
                },
                error: function (xhr) {
                    DevExpress.ui.notify(xhr.responseJSON?.message || "An error occurred.", "error", 2000);
                },
            });
        } else {
            DevExpress.ui.notify("Please fill required details!", "error", 2000);
        }
    }


</script>

