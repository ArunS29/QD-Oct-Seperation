@page
@model QD.ERP.Web.Areas.Finance.Pages.IndexModel
@using Newtonsoft.Json
@{
    ViewData["Title"] = "Outstanding Details";
}
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.4/Chart.js"></script>
<script src="https://www.gstatic.com/charts/loader.js"></script>
<div class="title">
    <div class="header-container">
        <div id="OutstandingDetails" class="no-border-title"></div>
    </div>
</div><!-- DevExtreme Scripts & Styles -->
<link rel="stylesheet" href="https://cdn3.devexpress.com/jslib/latest/css/dx.light.css">
<script src="https://cdn3.devexpress.com/jslib/latest/js/dx.all.js"></script>

<!-- Include Excel Export Libraries -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/exceljs/4.3.0/exceljs.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/FileSaver.js/2.0.5/FileSaver.min.js"></script>

<!-- Total Outstanding Cards -->
<div class="dx-card-container">
    <!-- Total Client Outstanding Card -->
    <div class="dx-card" id="clientcard">
        <div class="dx-card-header">
            <div id="TotalClientOutstanding" class="no-border-input"></div>
        </div>
        <div class="dx-card-content">
            <div id="totalClientOutstandingValue" class="total-value"></div>
        </div>
    </div>
    <!-- New Cash Balance Card -->
    <div class="dx-card" id="DashboardBankAccount">
        <div class="dx-card-header">
            <div id="TotalCashBalance" class="no-border-input"></div>
        </div>
        <div class="dx-card-content">
            <div id="cashBalanceValue" class="total-value"></div>
        </div>
    </div>

    <!-- Total Bills Outstanding Card -->
    <div class="dx-card" id="SupplierCard">
        <div class="dx-card-header">
            <div id="TotalSupplierOutstanding" class="no-border-input"></div>
        </div>
        <div class="dx-card-content">
            <div id="totalBillsOutstandingValue" class="total-value"></div>
        </div>
    </div>
</div>
<div class="grid-container">
    <!-- Client Outstanding Section -->
    <div class="grid-section">
        <div id="ClientOutstanding" class="no-border-title"></div>
        <div id="clientOutstandingGrid"></div>
    </div>

    <!-- Bills Payable Outstanding Section -->
    <div class="grid-section">
        <div id="SupplierOutstanding" class="no-border-title"></div>
        <div id="billsPayableGrid"></div>
    </div>

    <!-- Bank Account Details Section -->
    <div class="grid-section">
        <div id="BankAccountDetails" class="no-border-title"></div>
        <div id="accountSummaryGrid"></div>
    </div>
</div>

<div class="chart-row1">
    <canvas id="outstandingChart" style="width:100%;max-width:700px"></canvas>
    <div id="myChart2" style="width: 70%; height: 350px;"></div>
    
</div>
@* <div class="chart-row">
    <canvas id="myChart1" style="width:100%;max-width:700px"></canvas>

</div> *@
<!-- Pie Chart Containers -->
@* <div class="chart-row"> *@
@*     <div class="chart-container"> *@
@*         <h3>Client Outstanding Distribution</h3> *@
@*         <div id="clientOutstandingPieChart"></div> *@
@*     </div> *@
@*     <div class="chart-container"> *@
@*         <h3>Supplier Outstanding Distribution</h3> *@
@*         <div id="clientOutstandingPieChart1"></div> *@
@*     </div> *@
@*     <div class="chart-container"> *@
@*         <h3>Bank Account Distribution</h3> *@
@*         <div id="bankAccountsPieChart"></div> *@
@*     </div> *@
@* </div> *@
<!-- Grid Container -->


<!-- Charts Row Container -->
 @* <div class="chart-row">
    <!-- Client Outstanding Aging Chart -->
    <div class="chart-container">
        <div id="ClientOutstandingAgingChart" class="no-border-title"></div>
        <canvas id="clientOutstandingChart" width="600" height="400"></canvas>
    </div>

    <!-- Supplier Outstanding Aging Chart -->
    <div class="chart-container">
        <div id="SupplierOutstandingAgingChart" class="no-border-title"></div>
        <canvas id="billsOutstandingChart" width="600" height="400"></canvas>
        <div id=""></div>
    </div>

    <!-- Bank Accounts Chart -->
    <div class="chart-container">
        <div id="BankAccountsChart" class="no-border-title"></div>
        <div id="bankAccountsChart"></div>
    </div>
</div>  *@
<script>
    //Dashboardclient
         $("#clientcard").on("click", function () {
            // Your click logic here
         window.location.href = `/${tenantName}/Finance/DashboardClient`;    });

   //DashboardSupplier
        $("#SupplierCard").on("click", function () {
    // Your click logic here
    window.location.href = `/${tenantName}/Finance/DashboardSupplier`;    });

    //Dashboardbankaccount
            $("#DashboardBankAccount").on("click", function () {
        // Your click logic here
        window.location.href = `/${tenantName}/Finance/DashboardBankAccount`;    });

    $(document).ready(function () {
        //     $.ajax({
        //     url: "/api/Dashboard/GetOutstandingChartData",
        //     type: "GET",
        //     dataType: "json",
        //     success: function (response) {
        //         if (response.success) {
        //             const data = response.data;

        //             // Separate client and supplier data
        //             const clientData = data.filter(item => item.Type === "Client");
        //             const supplierData = data.filter(item => item.Type === "Supplier");

        //             // Aggregate data for ranking
        //             const accountBalances = [...new Set(data.map(item => item.AccountHead))].map(label => {
        //                 const clientBalance = clientData.find(d => d.AccountHead === label)?.Balance || 0;
        //                 const supplierBalance = supplierData.find(d => d.AccountHead === label)?.Balance || 0;
        //                 return {
        //                     label: label,
        //                     clientBalance: clientBalance,
        //                     supplierBalance: supplierBalance,
        //                     totalBalance: clientBalance + supplierBalance
        //                 };
        //             });

        //             // Sort by total balance and take top 10
        //             const topAccounts = accountBalances
        //                 .sort((a, b) => b.totalBalance - a.totalBalance) // Descending order
        //                 .slice(0, 10); // Top 10

        //             // Extract labels and data for the chart
        //             const labels = topAccounts.map(item => item.label);
        //             const clientBalances = topAccounts.map(item => item.clientBalance);
        //             const supplierBalances = topAccounts.map(item => item.supplierBalance);

        //             // Example third dataset (optional)
        //             const otherData = labels.map((_, index) => index * 500);

        //             // Create the chart
        //             new Chart("myChart1", {
        //                 type: "line",
        //                 data: {
        //                     labels: labels, // Account Names
        //                     datasets: [
        //                         {
        //                             label: formatMessage("Client Outstanding"),
        //                             data: clientBalances,
        //                             borderColor: "red",
        //                             fill: false
        //                         },
        //                         {
        //                             label: formatMessage("Supplier Outstanding"),
        //                             data: supplierBalances,
        //                             borderColor: "green",
        //                             fill: false
        //                         },
        //                         {
        //                             label: formatMessage("Other Data"),
        //                             data: otherData,
        //                             borderColor: "blue",
        //                             fill: false
        //                         }
        //                     ]
        //                 },
        //                 options: {
        //                     plugins: {
        //                         legend: { display: true }
        //                     },
        //                     scales: {
        //                         y: { beginAtZero: true }
        //                     }
        //                 }
        //             });
        //         } else {
        //             console.error("Error fetching data");
        //         }
        //     },
        //     error: function (error) {
        //         console.error("Error fetching data: ", error);
        //     }
        // });
            // $.ajax({
            //     url: "/api/Dashboard/GetClientOutstandingByOverdueDays",
            //     type: "GET",
            //     dataType: "json",
            //     success: function (response) {
            //         if (response.success) {
            //             // Extract data for the chart
            //             const labels = response.data.map(item => `${item.AccountHead} (${item.OverdueDays} Days)`);
            //             const balances = response.data.map(item => item.Balance);

            //             // Render the bar chart
            //             const ctx = document.getElementById("clientOutstandingChart").getContext("2d");
            //             new Chart(ctx, {
            //                 type: "bar",
            //                 data: {
            //                     labels: labels,
            //                     datasets: [{
            //                         label: "Balance Amount",

            //                         data: balances,
            //                         backgroundColor: "#007acc", // Updated color
            //                         borderColor: "#007acc", // Updated border color
            //                         borderWidth: 1
            //                     }]
            //                 },
            //                 options: {
            //                     responsive: true,
            //                     plugins: {
            //                         legend: {
            //                             display: true,
            //                             labels: {
            //                                 font: {
            //                                     size: 12 // Adjust legend font size
            //                                 }
            //                             }
            //                         },
            //                         tooltip: {
            //                             enabled: true,
            //                             callbacks: {
            //                                 label: function (context) {
            //                                     return `Balance: ${context.raw.toLocaleString()}`;
            //                                 }
            //                             }
            //                         },
            //                         title: {
            //                             display: true,
            //                             text: "Client Outstanding Balances by Overdue Days", // Chart title
            //                             font: {
            //                                 size: 16,
            //                                 weight: "bold"
            //                             }
            //                         }
            //                     },
            //                     scales: {
            //                         x: {
            //                             title: {
            //                                 display: true,
            //                                 text: "Clients (Overdue Days)", // X-axis title
            //                                 font: {
            //                                     size: 14,
            //                                     weight: "bold"
            //                                 }
            //                             },
            //                             ticks: {
            //                                 autoSkip: false, // Prevent skipping labels
            //                                 maxRotation: 45, // Rotate labels for better readability
            //                                 minRotation: 0
            //                             }
            //                         },
            //                         y: {
            //                             beginAtZero: true,
            //                             title: {
            //                                 display: true,
            //                                 text: "Balance Amount", // Y-axis title
            //                                 font: {
            //                                     size: 14,
            //                                     weight: "bold"
            //                                 }
            //                             },
            //                             ticks: {
            //                                 callback: function (value) {
            //                                     return value.toLocaleString(); // Format Y-axis values
            //                                 }
            //                             }
            //                         }
            //                     }
            //                 }
            //             });
            //         } else {
            //             console.error("Error loading data: ", response.message);
            //         }
            //     },
            //     error: function (xhr, status, error) {
            //         console.error("Error fetching data: ", error);
            //     }
            // });
            //
            // $.ajax({
            //     url: "/api/Dashboard/GetBankAccounts",
            //     type: "GET",
            //     dataType: "json",
            //     success: function (response) {
            //         $("#bankAccountsChart").dxChart({
            //             dataSource: response.data,
            //             series: [{
            //                 argumentField: "AccountHeadName",
            //                 valueField: "Amount",
            //                 name: formatMessage("Amount"),
            //                 type: "bar",
            //                 color: "#007acc"
            //             }],
            //             argumentAxis: {
            //                 label: { rotationAngle: -45 },
            //                 title: formatMessage("Account Head Name")

            //             },
            //             valueAxis: {
            //                 title: formatMessage("Amount"),
            //                 label: { format: { type: "fixedPoint", precision: 2 } }
            //             },
            //             legend: { visible: true },
            //             tooltip: {
            //                 enabled: true,
            //                 format: { type: "fixedPoint", precision: 2 }
            //             }
            //         });
            //     },
            //     error: function (xhr, status, error) {
            //         console.error("Error fetching data: ", error);
            //     }
            // });
            // Fetch Client Outstanding Aging Data
            // $.ajax({
            //     url: "/api/Dashboard/GetClientOutstandingAgingForChart",
            //     type: "GET",
            //     dataType: "json",
            //     success: function (response) {
            //         if (response.success) {
            //             // Client Outstanding Pie Chart
            //             $("#clientOutstandingPieChart").dxPieChart({
            //                 dataSource: response.agingData,
            //                 series: [{
            //                     argumentField: "OverDueGroup",
            //                     valueField: "Balance",
            //                     label: {
            //                         visible: true,
            //                         format: "currency"
            //                     }
            //                 }],
            //                title:formatMessage( "Client Outstanding Distribution")
            //             });
            //         }
            //     }
            // });
    //         $.ajax({
    //     url: "/api/Dashboard/GetBillsOutstandingAgingForChart",
    //     type: "GET",
    //     dataType: "json",
    //     success: function (response) {
    //         if (response.success) {
    //             // Client Outstanding Pie Chart
    //             $("#clientOutstandingPieChart1").dxPieChart({
    //                 dataSource: response.data,
    //                 series: [{
    //                     argumentField: "OverdueDays",
    //                     valueField: "Balance",
    //                     label: {
    //                         visible: true,
    //                         format: "currency"
    //                     }
    //                 }],
    //                 title: formatMessage("Supplier Outstanding Distribution"),
    //             });
    //         }
    //     }
    // });
            // Fetch Bank Accounts Data
            // $.ajax({
            //     url: "/api/Dashboard/GetBankAccounts",
            //     type: "GET",
            //     dataType: "json",
            //     success: function (response) {
            //         // Bank Accounts Pie Chart
            //         $("#bankAccountsPieChart").dxPieChart({
            //             dataSource: response.data,
            //             series: [{
            //                 argumentField: "AccountHeadName",
            //                 valueField: "Amount",
            //                 label: {
            //                     visible: true,
            //                     format: "currency"
            //                 }
            //             }],
            //             title: formatMessage("Bank Account Distribution")
            //         });
            //     }
            // });
            // $.ajax({
            //     url: "/api/Dashboard/GetBillsOutstandingAgingForChart",
            //     type: "GET",
            //     dataType: "json",
            //     success: function (response) {
            //         console.log("API Response:", response);

            //         if (response.success && Array.isArray(response.agingData) && response.agingData.length > 0) {
            //             // Ensure Balance is a number
            //             response.agingData.forEach(item => {
            //                 item.Balance = Number(item.Balance) || 0;
            //             });

            //             var pieChartInstance = $("#billsOutstandingPieChart").dxPieChart("instance");

            //             if (pieChartInstance) {
            //                 // Update the existing chart
            //                 pieChartInstance.option("dataSource", response.agingData);
            //             } else {
            //                 // Initialize the pie chart
            //                 $("#billsOutstandingPieChart").dxPieChart({
            //                     dataSource: response.agingData,
            //                     series: [{
            //                         argumentField: "OverDueGroup",
            //                         valueField: "Balance",
            //                         label: {
            //                             visible: true,
            //                             format: "currency"
            //                         }
            //                     }],
            //                     title: formatMessage("Supplier Outstanding Distribution"),
            //                 });
            //             }
            //         } else {
            //             console.error("Invalid or empty data received from API.");
            //         }
            //     },
            //     error: function (xhr, status, error) {
            //         console.error("AJAX Error:", status, error);
            //     }
            // });
          $.ajax({
    url: "/api/Dashboard/GetOutstandingChartData",
    type: "GET",
    dataType: "json",
    success: function (response) {
        if (response.success) {
            const data = response.data;

            const clientData = data.filter(item => item.Type === "Client");
            const supplierData = data.filter(item => item.Type === "Supplier");

            const labels = [...new Set(data.map(item => item.AccountHead))];

            const clientBalances = labels.map(label => {
                const item = clientData.find(d => d.AccountHead === label);
                return item ? item.Balance : 0;
            });

            const supplierBalances = labels.map(label => {
                const item = supplierData.find(d => d.AccountHead === label);
                return item ? item.Balance : 0;
            });

            new Chart("outstandingChart", {
                type: "bar", // Correct chart type to "bar"
                data: {
                    labels: labels, // Set labels directly for x-axis
                    datasets: [
                        {
                            label: formatMessage("Client Outstanding"),
                            backgroundColor: "rgba(0, 123, 255, 0.5)",
                            borderColor: "rgba(0, 123, 255, 1)",
                            data: clientBalances, // Use the prepared balances directly
                        },
                        {
                            label: formatMessage("Supplier Outstanding"),
                            backgroundColor: "rgba(255, 99, 132, 0.5)",
                            borderColor: "rgba(255, 99, 132, 1)",
                            data: supplierBalances, // Use the prepared balances directly
                        }
                    ]
                },
                options: {
                    responsive: true,
                    scales: {
                        x: {
                            title: { display: true, text: formatMessage("Accounts") },
                            // Optionally, you can set the "category" scale if needed
                        },
                        y: {
                            title: { display: true, text: formatMessage("Outstanding Amount") }
                        }
                    }
                }
            });
        } else {
            console.error("Error fetching data");
        }
    },
    error: function (error) {
        console.error("Error fetching data: ", error);
    }
});

        });

    // Handle click on "Total Client Outstanding" card
    $("#totalClientOutstandingCard").click(function () {
        $(".dx-card-container, .grid-container, .chart-container").hide(); // Hide other sections
        $("#clientOutstandingSection").show(); // Show Client Outstanding
    });

    // Back button functionality
    $("#backToDashboard").click(function () {
        $("#clientOutstandingSection").hide(); // Hide Client Outstanding
        $(".dx-card-container, .grid-container, .chart-container").show(); // Show main dashboard
    });
            google.charts.load('current', { 'packages': ['corechart'] });
        google.charts.setOnLoadCallback(loadChartData);

        function loadChartData() {
            $.ajax({
                url: "/api/Dashboard/GetOutstandingChartData",
                type: "GET",
                dataType: "json",
                success: function (response) {
                    if (response.success) {
                        const data = response.data;

                        // Aggregate data for ranking
                        const accountBalances = [...new Set(data.map(item => item.AccountHead))].map(label => {
                            const totalBalance = data
                                .filter(item => item.AccountHead === label)
                                .reduce((sum, item) => sum + item.Balance, 0);

                            return { label: label, balance: totalBalance };
                        });

                        // Sort by balance and take top 5
                        const topAccounts = accountBalances
                            .sort((a, b) => b.balance - a.balance) // Descending order
                            .slice(0, 5); // Top 5

                        // Prepare data for Google Charts
                        const chartData = [['Account Head', 'Outstanding Balance']];
                        topAccounts.forEach(item => {
                            chartData.push([item.label, item.balance]);
                        });

                        drawChart(chartData);
                    } else {
                        console.error("Error fetching data");
                    }
                },
                error: function (error) {
                    console.error("Error fetching data: ", error);
                }
            });
        }

        function drawChart(chartData) {
            // Convert to Google Charts format
            const data = google.visualization.arrayToDataTable(chartData);

            // Set Options
            const options = {
                title: formatMessage('Top 5 Outstanding Accounts'),
                is3D: true,
                pieSliceText: 'value',
                height: 400
            };

            // Draw Chart
            const chart = new google.visualization.PieChart(document.getElementById('myChart2'));
            chart.draw(data, options);
        }
    $(function () {
        // Fetch Total Client Outstanding
        $.getJSON("/api/Dashboard/GetTotalClientOutstanding", function (response) {
            if (response.success) {
                $("#totalClientOutstandingValue").text(response.totalOutstanding.toLocaleString('en-US'));
            } else {
                alert("Error loading total client outstanding data.");
            }
        });

        // Fetch Total Bills Outstanding
        $.getJSON("/api/Dashboard/GetTotalBillsOutstanding", function (response) {
            if (response.success) {
                $("#totalBillsOutstandingValue").text(response.totalBillsOutstanding.toLocaleString('en-US'));
            } else {
                alert("Error loading total bills outstanding data.");
            }
        });

        // Client Outstanding DataGrid
          $(document).ready(function () {
        $.ajax({
            url: "/api/Dashboard/GetClientOutstanding",
            type: "GET",
            dataType: "json",
            success: function (response) {
                if (response && response.data) {
                    $("#clientOutstandingGrid").dxDataGrid({
                        dataSource: response.data,
                        columns: [
                            {
                                dataField: "AccountHead",
                                caption: formatMessage("Account Head"),
                                width: 200,
                                allowReordering: true,
                                headerCellTemplate: function (header, info) {
                                    $("<div>")
                                        .text(info.column.caption)
                                        .css({
                                            "text-align": "center",
                                            "font-weight": "bold"
                                        })
                                        .appendTo(header);
                                }
                            },
                            {
                                dataField: "Balance",
                                caption: formatMessage("Balance Amount"),
                                format: { type: "fixedPoint", precision: 2 },
                                allowReordering: true,
                                headerCellTemplate: function (header, info) {
                                    $("<div>")
                                        .text(info.column.caption)
                                        .css({
                                            "text-align": "center",
                                            "font-weight": "bold"
                                        })
                                        .appendTo(header);
                                }
                            },
                            {
                                dataField: "OverdueDays",
                                caption: formatMessage("Overdue Days"),
                                visible: false,
                                allowHiding: true
                            },
                            {
                                dataField: "AccountGroupId",
                                caption: formatMessage("Account Group"),
                                visible: false,
                                allowHiding: true
                            },
                            {
                                dataField: "AccountHeadNo",
                                caption: formatMessage("Account Head No"),
                                visible: false,
                                allowHiding: true
                            }
                        ],
                        columnAutoWidth: true,
                        showBorders: true,
                        filterRow: { visible: false },
                        paging: { enabled: false },
                        height: 400,
                        scrolling: {
                            mode: "virtual"
                        },
                        searchPanel: { visible: false },
                        allowColumnReordering: true,
                        allowColumnResizing: true,
                        columnChooser: {
                            enabled: true,
                            mode: "select"
                        },
                        toolbar: {
                            items: [
                                {
                                    location: "after",
                                    widget: "dxButton",
                                    options: {
                                        icon: "columnchooser",
                                        text: formatMessage("Column Chooser"),
                                        onClick: function () {
                                            $("#clientOutstandingGrid").dxDataGrid("instance").showColumnChooser();
                                        }
                                    }
                                }
                            ]
                        },
                        summary: {
                            totalItems: [
                                    {
                                        column: "Balance",
                                        summaryType: "sum",
                                        valueFormat: { type: "fixedPoint", precision: 2 },
                                        displayFormat: "Total: {0}"
                                    },
                                    {
                                        column: "AccountHead",
                                        summaryType: "count",
                                        displayFormat: "Count: {0}"
                                    }
                            ]
                        }
                    });
                } else {
                    console.error("No data found in the response.");
                }
            },
            error: function (xhr, status, error) {
                console.error("Error fetching data:", error);
            }
        });
    });


        // Bills Payable Outstanding DataGrid
          $(document).ready(function () {
        $.ajax({
            url: "/api/Dashboard/GetBillsPayableOutstanding",
            type: "GET",
            dataType: "json",
            success: function (response) {
                if (response && response.data) {
                    $("#billsPayableGrid").dxDataGrid({
                        dataSource: response.data,
                        columns: [
                            {
                                dataField: "AccountHead",
                                caption: formatMessage("Account Head"),
                                width: 200,
                                headerCellTemplate: function (header, info) {
                                    $("<div>")
                                        .text(info.column.caption)
                                        .css({
                                            "text-align": "center",
                                            "font-weight": "bold"
                                        })
                                        .appendTo(header);
                                }
                            },
                            {
                                dataField: "Balance",
                                caption: formatMessage("Balance Amount"),
                                format: { type: "fixedPoint", precision: 2 },
                                headerCellTemplate: function (header, info) {
                                    $("<div>")
                                        .text(info.column.caption)
                                        .css({
                                            "text-align": "center",
                                            "font-weight": "bold"
                                        })
                                        .appendTo(header);
                                }
                            },
                            {
                                dataField: "OverdueDays",
                                caption: formatMessage("Overdue Days"),
                                visible: false,
                                allowHiding: true
                            },
                            {
                                dataField: "AccountHeadNo",
                                caption: formatMessage("Account Head No"),
                                visible: false,
                                allowHiding: true
                            }
                        ],
                        columnAutoWidth: true,
                        showBorders: true,
                        filterRow: { visible: false },
                        paging: { enabled: false },
                        height: 400,
                        scrolling: {
                            mode: "virtual"
                        },
                        searchPanel: { visible: false },
                        allowColumnReordering: true,
                        allowColumnResizing: true,
                        columnChooser: {
                            enabled: true,
                            mode: "select"
                        },
                        toolbar: {
                            items: [
                                {
                                    location: "after",
                                    widget: "dxButton",
                                    options: {
                                        icon: "columnchooser",
                                        text: formatMessage("Column Chooser"),
                                        onClick: function () {
                                            $("#billsPayableGrid").dxDataGrid("instance").showColumnChooser();
                                        }
                                    }
                                }
                            ]
                        },
                        summary: {
                            totalItems: [
                                    {
                                        column: "Balance",
                                        summaryType: "sum",
                                        valueFormat: { type: "fixedPoint", precision: 2 },
                                        displayFormat: "Total: {0}"
                                    },
                                    {
                                        column: "AccountHead",
                                        summaryType: "count",
                                        displayFormat: "Count: {0}"
                                    }
                            ]
                        }
                    });
                } else {
                    console.error("No data found in the response.");
                }
            },
            error: function (xhr, status, error) {
                console.error("Error fetching data:", error);
            }
        });
    });




            $(document).ready(function () {
                // Fetch data using AJAX
                $.ajax({
                    url: "/api/Dashboard/GetAccountSummary", // API endpoint
                    type: "GET",
                    dataType: "json",
                    success: function (response) {
                        // Check if the response contains data
                        if (response && response.data) {
                            // Initialize the DataGrid with the fetched data
                            $("#accountSummaryGrid").dxDataGrid({
                                dataSource: response.data, // Use the data from the response
                                columns: [
                                    { dataField: "AccountHeadName", caption: "Account Head", width: 200, allowReordering: true },
                                        { dataField: "TotalBalance", caption: "Balance Amount", format: { type: "fixedPoint", precision: 2 }, allowReordering: true },
                                    { dataField: "AccountGroup", caption: "Account Group", visible: true, allowHiding: true }
                                ],
                                columnAutoWidth: true, // Automatically adjust column widths
                                showBorders: true, // Show grid borders
                                filterRow: { visible: true }, // Enable filter row
                                paging: { enabled: false }, // Disable paging to show all data

                                // Enable vertical scrolling
                                height: 400, // Adjust grid height as needed
                                scrolling: {
                                    mode: "virtual" // Enable virtual scrolling for large datasets
                                },

                                searchPanel: { visible: false },

                                // Enable column reordering and resizing
                                allowColumnReordering: true,    
                                allowColumnResizing: true,

                                // Enable column chooser
                                columnChooser: {
                                    enabled: true,
                                    mode: "select"
                                },

                                // Add Column Chooser button in toolbar
                                toolbar: {
                                    items: [
                                        {
                                            location: "after",
                                            widget: "dxButton",
                                            options: {
                                                icon: "columnchooser",
                                                text: "Column Chooser",
                                                onClick: function () {
                                                    $("#accountSummaryGrid").dxDataGrid("instance").showColumnChooser();
                                                }
                                            }
                                        }
                                    ]
                                },

                                // Add summary row
                                summary: {
                                    totalItems: [
                                            {
                                                column: "Balance Amount",
                                                summaryType: "sum",
                                                valueFormat: { type: "fixedPoint", precision: 2 },
                                                displayFormat: "Total: {0}"
                                            },
                                            {
                                                    column: "Account Head",
                                                summaryType: "count",
                                                displayFormat: "Count: {0}"
                                            }
                                    ]
                                }
                            });
                        } else {
                            console.error("No data found in the response.");
                        }
                    },
                    error: function (xhr, status, error) {
                        console.error("Error fetching data:", error);
                    }
                });
            });

        // // Bills Outstanding Aging Chart
        // $(document).ready(function () {
        //     $.ajax({
        //         url: "/api/Dashboard/GetBillsOutstandingAgingForChart",
        //         type: "GET",
        //         dataType: "json",
        //         success: function (response) {
        //             if (response.success) {
        //                 const labels = response.data.map(item => `${item.AccountHead || item.OverDueGroup}`);
        //                 const balances = response.data.map(item => item.Balance);

        //                 const ctx = document.getElementById("billsOutstandingChart").getContext("2d");
        //                 new Chart(ctx, {
        //                     type: "bar",
        //                     data: {
        //                         labels: labels,
        //                         datasets: [{
        //                             label: "Balance Amount",
        //                             data: balances,
        //                             backgroundColor: "#007acc",
        //                             borderColor: "#007acc",
        //                             borderWidth: 1
        //                         }]
        //                     },
        //                     options: {
        //                         responsive: true,
        //                         plugins: {
        //                             legend: {
        //                                 display: true,
        //                                 labels: {
        //                                     font: {
        //                                         size: 12
        //                                     }
        //                                 }
        //                             },
        //                             tooltip: {
        //                                 enabled: true,
        //                                 callbacks: {
        //                                     label: function (context) {
        //                                         return `Balance: ${context.raw.toLocaleString(undefined, { minimumFractionDigits: 2 })}`;
        //                                     }
        //                                 }
        //                             },
        //                             title: {
        //                                 display: true,
        //                                 text: "Top 5 Bills Outstanding by Overdue Group",
        //                                 font: {
        //                                     size: 16,
        //                                     weight: "bold"
        //                                 }
        //                             }
        //                         },
        //                         scales: {
        //                             x: {
        //                                 title: {
        //                                     display: true,
        //                                     text: "Overdue Groups",
        //                                     font: {
        //                                         size: 14,
        //                                         weight: "bold"
        //                                     }
        //                                 },
        //                                 ticks: {
        //                                     autoSkip: false,
        //                                     maxRotation: 45,
        //                                     minRotation: 0
        //                                 }
        //                             },
        //                             y: {
        //                                 beginAtZero: true,
        //                                 title: {
        //                                     display: true,
        //                                     text: "Balance Amount",
        //                                     font: {
        //                                         size: 14,
        //                                         weight: "bold"
        //                                     }
        //                                 },
        //                                 ticks: {
        //                                     callback: function (value) {
        //                                         return value.toLocaleString();
        //                                     }
        //                                 }
        //                             }
        //                         }
        //                     }
        //                 });
        //             } else {
        //                 console.error("Error loading data: ", response.message);
        //             }
        //         },
        //         error: function (xhr, status, error) {
        //             console.error("Error fetching data: ", error);
        //         }
        //     });
        // });

        $.getJSON("/api/Dashboard/GetCashBalance", function (response) {
            if (response.success) {
                    $("#cashBalanceValue").text(response.cashBalance.toLocaleString('en-US'));
            } else {
                alert("Error loading cash balance data.");
            }
        });
    });

   
             $(function () {
                                                         $("#OutstandingDetails").dxTextBox({
                                                                 text:formatMessage("Outstanding Details"),
                                                             readOnly: true,
                                                                 onContentReady: function(e) {
                                $(e.element).addClass("dx-textbox-heading");
                                    }
                                                     });
                                     });
             $(function () {
                                                     $("#TotalClientOutstanding").dxTextBox({
                                                             text:formatMessage("Total Client Outstanding"),
                                                         readOnly: true,
                                                             onContentReady: function(e) {
                            $(e.element).addClass("dx-textbox-heading10");
                                }
                                                 });
                                 });
                                     $(function () {
                                                         $("#TotalCashBalance").dxTextBox({
                                                                     text:formatMessage("Total Cash Balance"),
                                                             readOnly: true,
                                                                 onContentReady: function(e) {
                            $(e.element).addClass("dx-textbox-heading10");
                                }
                                                     });
                                     });
                                         $(function () {
                                                         $("#TotalSupplierOutstanding").dxTextBox({
                                                                     text:formatMessage("Total Supplier Outstanding"),
                                                             readOnly: true,
                                                                 onContentReady: function(e) {
                            $(e.element).addClass("dx-textbox-heading10");
                                }
                                                     });
                                     });

                                         $(function () {
                                                                 $("#ClientOutstanding").dxTextBox({
                                                                             text:formatMessage("Client Outstanding"),
                                                                 readOnly: true,
                                                                     onContentReady: function(e) {
                            $(e.element).addClass("dx-textbox-heading10");
                                }
                                                         });
                                         });
                                             $(function () {
                                                                 $("#SupplierOutstanding").dxTextBox({
                                                                             text:formatMessage("Supplier Outstanding"),
                                                                 readOnly: true,
                                                                     onContentReady: function(e) {
                            $(e.element).addClass("dx-textbox-heading10");
                                }
                                                         });
                                         });
                                             $(function () {
                                                                 $("#BankAccountDetails").dxTextBox({
                                                                             text:formatMessage("Bank Account Details"),
                                                                 readOnly: true,
                                                                     onContentReady: function(e) {
                            $(e.element).addClass("dx-textbox-heading10");
                                }
                                                         });
                                         });
                                             $(function () {
                                                                 $("#ClientOutstandingAgingChart").dxTextBox({
                                                                             text:formatMessage("Client Outstanding Aging Chart"),
                                                                 readOnly: true,
                                                                     onContentReady: function(e) {
                            $(e.element).addClass("dx-textbox-heading10");
                                }
                                                         });
                                         });
                                             $(function () {
                                                                 $("#SupplierOutstandingAgingChart").dxTextBox({
                                                                             text:formatMessage("Supplier Outstanding Aging Chart"),
                                                                 readOnly: true,
                                                                     onContentReady: function(e) {
                            $(e.element).addClass("dx-textbox-heading10");
                                }
                                                         });
                                         });
                                             $(function () {
                                                                 $("#BankAccountsChart").dxTextBox({
                                                                             text:formatMessage("Bank Accounts Chart"),
                                                                 readOnly: true,
                                                                     onContentReady: function(e) {
                            $(e.element).addClass("dx-textbox-heading10");
                                }
                                                         });
                                         });
</script>

<style>

    .chart-row1 {
        display: flex;
        justify-content: center;
        align-items: center;
    }

        .chart-row1 rect {
            fill: transparent;
        }

    .grid-section div {
        text-align: center; /* Center the title */
    }

    .dx-card-container {
        display: flex;
        justify-content: center;
        gap: 20px;
        padding: 1em;
    }

    .dx-card {
        display: inline-block;
        padding: 20px;
        background: #f5f5f5;
        border-radius: 10px;
        box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
        text-align: center;
    }

    .dx-card-header {
        font-size: 24px;
        font-weight: bold;
        margin-bottom: 10px;
        text-align: center;
    }

    .dx-card-content .total-value {
        font-size: 24px;
        font-weight: bold;
        color: #007acc;
    }

    .grid-container {
        display: flex;
        padding: 15px;
        justify-content: space-between;
    }

    .grid-section {
        width: 32%;
        text-align: center;
    }

    .chart-row {
        display: flex;
        justify-content: space-around;
        padding: 15px;
        align-items: center;
    }

    .chart-container {
        width: 32%;
        text-align: center;
    }

    #clientOutstandingChart,
    #billsOutstandingChart,
    #bankAccountsChart {
        width: 100%;
        height: 400px;
        margin: 0 auto;
    }

    .dx-card {
        padding: 20px;
        background: #f5f5f5;
        border-radius: 10px;
        box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
        text-align: center;
        width: 400px;
    }

    .dx-card-header {
        font-size: 18px;
        font-weight: bold;
        margin-bottom: 10px;
    }

    .dx-card-content .total-value {
        font-size: 24px;
        font-weight: bold;
        color: #007acc;
    }
</style>

