@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dynamic Breadcrumb Navigation</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400&display=swap&italic=true" rel="stylesheet">


    <style>
        /* Apply Inter Regular 400 Italic globally */
        * {
            color: var(--Global-Color-Grey-32, #525252); /* Default color */
            font-family: var(--Font-family-Base, 'Inter', sans-serif); /* Font family */
            font-size: var(--Font-size-300, 14px); /* Font size */
            font-style: normal; /* Font style */
            font-weight: 500; /* Font weight */
            line-height: var(--Line-height-300, 20px); /* Line height */
            -webkit-font-smoothing: antialiased; /* Smoothing for WebKit browsers */
            -moz-osx-font-smoothing: grayscale; /* Font smoothing for Firefox on macOS */
        }

        :root {
            font-family: "Inter", Helvetica, Arial, sans-serif;
            --highlight-color: #0f6cbd;
            --text-color: #525252;
            --hover-bg: #ebf3fc;
            --sidebar-bg: #fff;
            --breadcrumb-color: #808080;
        }

        body {
            font-family: var(--font-family);
            margin: 0;
            background-color: #f9f9f9;
            display: flex;
            height: 100vh;
            flex-direction: column; /* Stack breadcrumb and main content */
        }

        .container {
            display: flex;
            flex-grow: 1;
            padding:0px;
            margin-left:0px;
            margin-right:0px;
        }

        /* Sidebar 1 styles */
        .sidebar {
            width: 250px;
            background-color: var(--sidebar-bg);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
            border-right: 1px solid #ddd;
            padding: 16px;
            padding-left: 10px;
            padding-right: 10px;
            padding-top: 37px;
            margin-top: 47px;
            transition: width 0.3s ease;
            height: 690px;
        }

        /* Sidebar 2 styles */
        .sidebar2 {
            margin-top: 45px;
            margin-left: 0px transition: margin-left 0.3s ease;
            padding: 20px;
        }

        /* Hidden state of sidebar */
        .sidebar.hidden {
            width: 60px; /* Minimized width to show icons only */
        }

        /* Expanded state of sidebar 2 */
        .sidebar2.expanded {
            margin-left: 0px; /* Sidebar 2 moves left when sidebar1 is collapsed */
            margin-top: 45px;
        }

        /* Menu items */
        .menu-item {
            display: flex;
            align-items: center;
            padding: 16px 8px;
            gap: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        /* Hide text when sidebar is hidden */
        .sidebar.hidden .menu-item .text {
            display: none; /* Hide text */
        }

        /* Menu item hover effect */
        .menu-item:hover {
            background-color: var(--hover-bg);
            border-radius: 4px;
        }

        .menu-item i {
            width: 22.5px;
            height: 18.4px;
            font-size: 18px; /* Adjust as needed for consistent sizing */
            display: flex;
            align-items: center;
            justify-content: center;
        }


        .menu-item .text {
            color: var(--text-color);
            font-size: 14px;
            font-weight: 500;
        }

        /* Active menu item */
        .menu-item.active .text {
            color: var(--highlight-color);
        }

        /* Toggle button */
        .toggle-btn {
            position: absolute;
            top: 65px;
            left: calc(var(--sidebar-width, 260px)); /* Use the sidebar width */
            width: 30px;
            height: 30px;
            background-color: #ffffff;
            border: 1px solid #ddd;
            box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            z-index: 1000;
            transition: left 0.3s ease; /* Smooth transition */
          
        }

            .toggle-btn:hover {
                background-color: #e9ecef;
            }

        /* Sidebar hidden state */
        .sidebar.hidden ~ .toggle-btn {
            left: calc(var(--sidebar-width, 70px)); /* Adjust position dynamically */
        }



        /* Sidebar 2 styles */
        .sidebar2 {
            width: 250px;
            transition: background-color 0.3s ease;
            background-color: var(--sidebar-bg);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
            border-left: 1px solid #ddd;
            border-right: 1px solid #ddd; /* Right side border */
            padding: 8px 16px; /* Reduced padding */
            padding-top: 19px;
            margin-top: 75px; /* Adjust to align with the rest of your layout */
            height: calc(100vh - 75px); /* Sidebar takes full height minus the top margin */
            overflow-y: auto; /* Enable vertical scrolling */
            scrollbar-width: thin; /* For Firefox: thin scrollbar */
            scrollbar-color: #ccc #f9f9f9; /* For Firefox: color */
        }

            /* Scrollbar styles for Webkit-based browsers */
            .sidebar2::-webkit-scrollbar {
                width: 8px; /* Set the width of the scrollbar */
            }

            .sidebar2::-webkit-scrollbar-thumb {
                background-color: #ccc; /* Color of the scrollbar thumb */
                border-radius: 4px; /* Round edges */
            }

                .sidebar2::-webkit-scrollbar-thumb:hover {
                    background-color: #aaa; /* Darker color on hover */
                }

            .sidebar2::-webkit-scrollbar-track {
                background-color: #f9f9f9; /* Background color of the scrollbar track */
                border-radius: 4px;
            }


            /* Make the inner elements unbold */
            .sidebar2 .submenu-item {
                font-weight: normal;
                font-size: 15px; /* Remove bold styling */
            }

            .sidebar2 .text {
                text-align: left;
                font-weight: normal; /* Ensure text inside submenu items is not bold */
            }

        /* Breadcrumb styles */
        .breadcrumb {
            margin-left: 10px;
            font-size: 14px;
            color: var(--breadcrumb-color);
            padding-left: 35px; /* Add some left padding */
            display: flex;
            justify-content: flex-start;
            align-items: center;
            gap: 8px;
            height: 30.5px;
            position: absolute;
            top: 65px; /* Align it to the top */
            left: calc(var(--sidebar-width, 256px)); /* Dynamically adjusts based on sidebars */
            right: 0; /* Stretches to full width of the screen */
            background-color: #fff;
            z-index: 10;
            flex-wrap: nowrap;
            border: 1px solid #ddd;
            transition: left 0.3s ease, width 0.3s ease; /* Smooth transition */
        }

            /* Breadcrumb item styles */
            .breadcrumb span {
                cursor: pointer;
                font-weight: 500;
                color: var(--text-color);
            }

            .breadcrumb .separator {
                color: #aaa;
                font-size: 14px;
            }

            .breadcrumb span:hover {
                color: var(--highlight-color);
                text-decoration: underline;
            }




        /* Submenu styles */
        .submenu-item {
            display: flex;
            align-items: center;
            padding: 10px 16px;
            gap: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            margin-left: 16px;
        }

            .submenu-item:hover {
                background-color: var(--hover-bg);
                border-radius: 4px;
            }

            .submenu-item i {
                font-size: 14px;
                width: 14px;
                height: 14px;
            }


        .main-content {
            display: flex;
            flex-grow: 1;
            height: 100%; /* Full height */
            position: relative;
            overflow: hidden;
            transition: width 0.3s ease; /* Smooth width transition */
            margin-top: 26px;
           
        }

        iframe {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%; /* Occupy full width of parent */
            height: 100%; /* Occupy full height of parent */
            border: none;
        }


        /* Previous Button Style */
        .prev-btn {
            background-color: transparent;
            color: #ebf3fc;
            border:;
            border-radius: 50%;
            width: 20px;
            height: 20px;
            font-size: 10px;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            transition: background-color 0.3s;
        }

            .prev-btn:hover {
                background-color: lightslategrey;
            }

        .frame {
            display: flex; /* Use flexbox to align items */
            align-items: center; /* Vertically align items */
            justify-content: flex-end; /* Push items to the right */
            width: 100%; /* Occupy the full width of the screen */
            padding: 10px; /* Add some spacing */
            padding-bottom: 0px;
            background-color: white; /* Optional background color */
            position: fixed; /* Make it fixed to stay visible on scroll */
            top: 0; /* Position at the top */
            left: 0; /* Start from the very left */
            z-index: 100; /* Ensure it stays above other elements */
            height: 55px;
            border-bottom: 1px solid #ddd;
        }


            /* Style for the icons */
            .frame .img {
                font-size: 20px; /* Set size of icons */
                margin: 0 5px; /* Add spacing between icons */
                color: #333; /* Set icon color */
                cursor: pointer; /* Add pointer cursor for hover effect */
            }

            /* Style for the avatar */
            .frame .avatar {
                width: 20px;
                height: 20px;
                background-color: #ccc; /* Placeholder color */
                border-radius: 50%; /* Make it circular */
                border: 2px solid #333; /* Optional border */
                margin-left: 10px;
                margin-right: 15px; /* Spacing from the last icon */
            }
            /* Logo styling inside the frame */
            .frame .image {
                display: flex; /* Flex container for logo */
                align-items: center; /* Center-align vertically */
                margin-right: auto; /* Push other elements to the right */
                margin-bottom: 10px;
                padding-left: 30px;
            }

                .frame .image img {
                    width: 140px; /* Adjust logo size */
                    height: 30; /* Maintain aspect ratio */
                }
        /*  /* Loader styling */
        .loader {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            z-index: 1000; /* Ensure it's above all elements */
            display: none; /* Hidden by default */
        }

            .loader img {
                width: 500px; /* Adjust size as needed */
                height: 500px;
            }

        .menu i {
            color: darkgrey;
        }

        .financeDashboardSubmenu {
            height: 20px;
            width: 114px;
        }
        /* Base styling for all popups */
        .popup {
            display: none; /* Hidden by default */
            position: absolute; /* Make it float above the content */
            top: 10%;
            right: 10%;
            width: 300px;
            background-color: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            z-index: 9999;
            border-radius: 8px;
            padding: 20px;
        }

        .popup-header {
            font-size: 18px;
            font-weight: bold;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }

        .popup-close {
            font-size: 20px;
            cursor: pointer;
            color: #888;
        }

            .popup-close:hover {
                color: #333;
            }

        .popup-content {
            font-size: 16px;
            color: #333;
        }

        /* Styling for the avatar (User Profile Button) */
        .avatar {
            width: 40px;
            height: 40px;
            background-color: #ccc;
            border-radius: 50%;
            cursor: pointer;
        }

        /* Special styling for the Profile Popup */
        .profile-popup {
            width: 350px;
            top: 15%;
            right: 5%;
        }





        /* Ensure iframe takes full size */
        #contentIframe {
            width: 100% !important;
            height: 100% !important;
            display: block;
        }



        /* Add any other profile styling as needed */



    
    </style>
</head>
<body>
    <div class="frame">
        <div class="image">
            <img src="~/images/Pulse.png" alt="Pulse Logo" />
        </div>

        <i class="fas fa-bell img" onclick="handleBellClick()"></i> <!-- Bell icon -->
        <i class="fas fa-cogs img" onclick="handleGearClick()"></i> <!-- Gear icon -->
        <i class="fas fa-file-alt img" onclick="handleFileClick()"></i> <!-- File text icon -->
        <div class="avatar" onclick="handleAvatarClick()"></div> <!-- Avatar -->

    </div>
    <!-- Notification Popup -->
    <div id="notificationPopup" class="popup">
        <div class="popup-header">
            Notifications
            <span class="popup-close" onclick="closePopup('notificationPopup')">X</span>
        </div>
        <div class="popup-content">
            <!-- Add notification content here -->
            <p>You have new notifications!</p>
        </div>
    </div>

    <!-- Settings Popup -->
    <div id="settingsPopup" class="popup">
        <div class="popup-header">
            Settings
            <span class="popup-close" onclick="closePopup('settingsPopup')">X</span>
        </div>
        <div class="popup-content">
            <!-- Add settings content here -->
            <p>Adjust your preferences here.</p>
        </div>
    </div>

    <!-- Profile Popup -->
    <div id="profilePopup" class="popup profile-popup">
        <div class="popup-header">
            User Profile
            <span class="popup-close" onclick="closePopup('profilePopup')">X</span>
        </div>
        <div class="popup-content">
            <!-- Add user profile content here -->
            <p>Profile details go here...</p>
        </div>
    </div>




    <!-- Main container to hold sidebars and content -->
    <div class="container">
        <!-- Sidebar 1 Section -->
        <!-- Sidebar Header with Logo -->

        <div id="sidebar" class="sidebar">
            <div class="menu">
                <div class="menu-item" onclick="navigate('Human Resources')">
                    <i class="fas fa-users"></i>
                    <div class="text">Human Resources</div>
                </div>
                <div class="menu-item" onclick="navigate('Finance')">
                    <i class="fas fa-dollar-sign"></i>
                    <div class="text">Finance</div>
                </div>
                <div class="menu-item" onclick="navigate('VAT Modules')">
                    <i class="fas fa-certificate"></i>
                    <div class="text">VAT Modules</div>
                </div>
                <div class="menu-item" onclick="navigate('Equipment Rentals')">
                    <i class="fas fa-truck"></i>
                    <div class="text">Equipment Rentals</div>
                </div>
                <div class="menu-item" onclick="navigate('Camp Management')">
                    <i class="fas fa-campground"></i>
                    <div class="text">Camp Management</div>
                </div>
                <div class="menu-item" onclick="navigate('Inventory Management')">
                    <i class="fas fa-cogs"></i>
                    <div class="text">Inventory Management</div>
                </div>
                <div class="menu-item" onclick="navigate('Projects Control')">
                    <i class="fas fa-project-diagram"></i>
                    <div class="text">Projects Control</div>
                </div>
            </div>
        </div>

        <div id="sidebar2" class="sidebar2">
            <!-- Content for Sidebar 2 -->
        </div>

        <div>
            <button class="toggle-btn" onclick="toggleSidebar()">&#x3c;</button>
        </div>

        <!-- Breadcrumb above Sidebar 2 -->
        <div class="breadcrumb" id="breadcrumb">
          @*   <button class="prev-btn" onclick="goBack()">&#60;</button> *@
            <span onclick="resetBreadcrumb()">Home</span>
        </div>

        <div class="main-content" id="mainContent">
            <iframe id="contentIframe"
                    src=""
                    frameborder="0"></iframe>
        </div>

    </div>
    <!-- Main Content -->

 
                    @*   <div id="loader" class="loader">
    <img src="~/images/Loading.gif" alt="Loading..." />
    </div>
    <iframe id="contentIframe" onload="hideLoader()" style="display: none;"></iframe *@




    <script>
               // Function for Bell icon click - show notifications popup
        function handleBellClick() {
            const notificationPopup = document.getElementById('notificationPopup');
            if (notificationPopup) {
                notificationPopup.style.display = 'block';
            }
        }

        // Function for Gear icon click - show settings popup
        function handleGearClick() {
            const settingsPopup = document.getElementById('settingsPopup');
            if (settingsPopup) {
                settingsPopup.style.display = 'block';
            }
        }

        // Function for Avatar click - show user profile
        function handleAvatarClick() {
            const profilePopup = document.getElementById('profilePopup');
            if (profilePopup) {
                profilePopup.style.display = 'block';
            }
        }

        // Function to close the popup
        function closePopup(popupId) {
            const popup = document.getElementById(popupId);
            if (popup) {
                popup.style.display = 'none';
            }
        }

        const breadcrumbTrail = []; // Tracks the breadcrumb trail

      function navigate(name) {
        
    const sidebar2 = document.getElementById('sidebar2');
    
    // Toggle sidebar2 visibility based on whether it's already visible or not
    if (sidebar2.style.display === 'block') {
        sidebar2.style.display = 'none'; // Hide sidebar2
    } else {
        sidebar2.style.display = 'block'; // Show sidebar2
    }

    // Dynamically load submenu items for the first sidebar menu
    if (name === 'Finance') {
        showFinanceSubmenu();
    } else {
      /*   sidebar2.innerHTML = ''; */ // Clear sidebar2 for other menus
    }

    // Ensure the first breadcrumb is always from Sidebar 1
    breadcrumbTrail[0] = name;
   /*  breadcrumbTrail.length = 1; */ // Reset subsequent breadcrumbs
    updateBreadcrumb();
}


        function showFinanceSubmenu() {
            const sidebar2 = document.getElementById('sidebar2');
            sidebar2.innerHTML = `
                      <div class="submenu-item" onclick="toggleSubmenu('financeDashboardSubmenu')" role="button" aria-expanded="false">
            <i class="fas fa-book-open"></i> <!-- Font Awesome icon -->
            <div class="text">Finance Dashboard</div>
        </div>
         <div id="financeDashboardSubmenu" class="nested-submenu" style="display: none; margin-left: 30px;">
         <div class="submenu-item" onclick="updateBreadcrumbFromSidebar2('/ My Dashboard')">
                <div class="text">My Dashboard</div>
            </div>
            <div class="submenu-item" onclick="loadVoucherVerification()">
                <div class="text">Voucher Verification</div>
            </div>
            <div class="submenu-item" onclick="loadBillsReceivable()">
                <div class="text">Bills Receivable</div>
            </div>
            <div class="submenu-item" onclick="loadBillsPayable()">
                <div class="text">Bills Payable</div>
            </div>
            <div class="submenu-item" onclick="loadCostAnalysis()">
                <div class="text">Cost Analysis</div>
            </div>
            <div class="submenu-item" onclick="loadSalaryPayable()">
                <div class="text">Salary Payable</div>
            </div>
            <div class="submenu-item" onclick="loadCostReportsByBranch()">
                <div class="text">Cost Report By Branch</div>
            </div>
            <div class="submenu-item" onclick="loadChartOfAccounts()">
                <div class="text">Chart Of Accounts</div>
            </div>
            <div class="submenu-item" onclick="loadChartofCostCenters()">
                <div class="text">Chart of Cost Centers</div>
            </div>
            <div class="submenu-item" onclick="loadFinancialReports()">
                <div class="text">Financial Reports</div>
            </div>
            <div class="submenu-item" onclick="loadAssestRegister()">
                <div class="text">Asset Register</div>
            </div>
                       <div class="submenu-item" onclick="assetallocation()">
                        <div class="text">Asset Allocation</div>
                    </div>
        </div>
                   
              
                   
                     @**@
        <div class="submenu-item" onclick="loadAccountLedger()">
            <i class="fas fa-book"></i>
            <div class="text">Accounting Ledgers</div>
        </div>
        <div class="submenu-item" onclick="loadAccountRegister()">
        <i class="fas fa-book"></i>
        <div class="text">Accounting Register</div>
        </div>
        <div class="submenu-item" onclick="loadBankPayment('Bank Payment')">
            <i class="fas fa-university"></i>
            <div class="text">Bank Payment</div>
        </div>
                 <div class="submenu-item" onclick="loadChartOfAccounts('/ Chart of Accounts')">
        <i class="fas fa-chart-pie"></i>
        <div class="text">Chart of Accounts</div>
        </div>
        <div class="submenu-item" onclick="loadChartofCostCenters('/ Chart of Cost Centers')">
        <i class="fas fa-chart-pie"></i>
        <div class="text">Chart of Cost Centers</div>
        </div>
        <div class="submenu-item" onclick="loadCashPayment('Cash Payment')">
            <i class="fas fa-money-bill-wave"></i>
            <div class="text">Cash Payment</div>
        </div>
        <div class="submenu-item" onclick="loadCashReceipt()">
            <i class="fas fa-dollar-sign"></i>
            <div class="text">Cash Receipt</div>
        </div>
        <div class="submenu-item" onclick="loadBankReceipt()">
            <i class="fas fa-receipt"></i>
            <div class="text">Bank Receipt</div>
        </div>
                        <div class="submenu-item" onclick="loadsalesvoucher()">
            <i class="fas fa-tags"></i>
            <div class="text">Sales Voucher</div>
        </div>
        <div class="submenu-item" onclick="loadPurchaseVoucher()">
            <i class="fas fa-shopping-cart"></i>
            <div class="text">Purchase Voucher</div>
        </div>
        <div class="submenu-item" onclick="loadJournelVoucher()">
            <i class="fas fa-book-open"></i>
            <div class="text">Journel Voucher</div>
        </div>
        <div class="submenu-item" onclick="loadExpenseClaim()">
            <i class="fas fa-file-invoice-dollar"></i>
            <div class="text">Expense Claim</div>
        </div>
        <div class="submenu-item" onclick="loadJournelRegister()">
            <i class="fas fa-newspaper"></i>
            <div class="text">Journel Register</div>
        </div>

      


                    `;
        }

        // Function to toggle visibility of nested submenus
        function toggleSubmenu(submenuId) {
            const submenu = document.getElementById(submenuId);
            if (submenu.style.display === 'none') {
                submenu.style.display = 'block';
            } else {
                submenu.style.display = 'none';
            }
        }

        // Function to toggle visibility of nested submenus
        function toggleSubmenu(submenuId) {
            const submenu = document.getElementById(submenuId);
            if (submenu.style.display === 'none') {
                submenu.style.display = 'block';
            } else {
                submenu.style.display = 'none';
            }
        }


               function loadAccountLedger() {
            // Set the iframe source to load Accounting Ledgers content
            const iframe = document.getElementById("contentIframe");
            iframe.src = "/Finance/AccountingLedgers";
            // Update the breadcrumb (same as the anchor tag in the menu)
            updateBreadcrumbFromSidebar2('/ Accounting Ledgers');

            const sidebar2 = document.getElementById('sidebar2');


                            sidebar2.style.display = 'none'; // Hide sidebar2



                             

                             autocollaps ();
                                            
        }



        function loadAccountRegister() {
            const iframe = document.getElementById("contentIframe");
            iframe.src = "/Finance/AccountingRegister";
            updateBreadcrumbFromSidebar2('/ Accounting Registers');
        }
                function loadVoucherVerification() {
                            const iframe = document.getElementById("contentIframe");
                                    iframe.src = "/Finance/VoucherApproval";
                            updateBreadcrumbFromSidebar2('/ Voucher Verification');
                        }
                function loadBillsReceivable() {
                    const iframe = document.getElementById("contentIframe");
                            iframe.src = "/Finance/BillsReceivable";
                    updateBreadcrumbFromSidebar2('/ Bills Receivable');
                }
                           function loadBillsPayable() {
                            const iframe = document.getElementById("contentIframe");
                                    iframe.src = "/Finance/BillsPayable";
                            updateBreadcrumbFromSidebar2('/ Bills Payable');
                        }
                                   function loadCostAnalysis() {
                            const iframe = document.getElementById("contentIframe");
                                    iframe.src = "/Finance/CostAnalysis";
                            updateBreadcrumbFromSidebar2('/ Cost Analysis');
                        }
                                   function loadBillsReceivable() {
                            const iframe = document.getElementById("contentIframe");
                                    iframe.src = "/Finance/BillsReceivable";
                            updateBreadcrumbFromSidebar2('/ Bills Receivable');
                        }
                        //             function loadSalaryPayable() {
                        //     const iframe = document.getElementById("contentIframe");
                        //             iframe.src = "/Finance/SalaryPayable";
                        //     updateBreadcrumbFromSidebar2('/ Salary Payable');
                        // }
 
                         

                         function loadFinancialReports() {
                                    const iframe = document.getElementById("contentIframe");
                                    iframe.src = "/Finance/uc20106TrialBalance";
                                    updateBreadcrumbFromSidebar2('/ Financial Reports');
                                }
                                 function loadAssestRegister() {
                                   const iframe = document.getElementById("contentIframe");
                                            iframe.src = "/Finance/AssetView";
                                    updateBreadcrumbFromSidebar2('/ Asset Register');
                                }
        function assetallocation() {
            const iframe = document.getElementById("contentIframe");
            iframe.src = "/Finance/AssetAllocation";
            updateBreadcrumbFromSidebar2('/ Asset Allocation');
        }
        function loadChartOfAccounts() {
            const iframe = document.getElementById("contentIframe");
            iframe.src = "/Finance/ChartOfAccounts";
            updateBreadcrumbFromSidebar2('/ Chart Of Accounts');
        }
         function loadChartofCostCenters() {
            const iframe = document.getElementById("contentIframe");
            iframe.src = "/Finance/ChartOfCostCenters";
            updateBreadcrumbFromSidebar2('/ Chart Of Cost Centers');
        }
                         function loadCashReceipt() {
                    const iframe = document.getElementById("contentIframe");
                            iframe.src = "/Finance/VoucherEntryCashReceipts";
                    updateBreadcrumbFromSidebar2('/ Cash Receipt');
                }
                        function loadBankReceipt() {
                            const iframe = document.getElementById("contentIframe");
                                            iframe.src = "/Finance/VoucherEntryReceipts";
                            updateBreadcrumbFromSidebar2('/ Bank Receipt');
                        }
        function loadsalesvoucher() {
            const iframe = document.getElementById("contentIframe");
            iframe.src = "/Finance/salesVoucher";
            updateBreadcrumbFromSidebar2('/ sales voucher');
        }

        function loadPurchaseVoucher() {
            const iframe = document.getElementById("contentIframe");
            iframe.src = "/Finance/purchasevoucher";
            updateBreadcrumbFromSidebar2('/ Purchase voucher');
        }
        function loadBankPayment() {
                                    const iframe = document.getElementById("contentIframe");
                                                    iframe.src = "/Finance/VoucherBankPayment";
                                    updateBreadcrumbFromSidebar2('/ Bank Payment');
                                }
                                                                function loadCashPayment() {
                                    const iframe = document.getElementById("contentIframe");
                                                    iframe.src = "/Finance/VoucherCashPayment";
                                    updateBreadcrumbFromSidebar2('/ Cash Payment');
                                }
                                                                     function loadExpenseClaim() {
                                            const iframe = document.getElementById("contentIframe");
                                                            iframe.src = "/Finance/ExpensesClaims";
                                            updateBreadcrumbFromSidebar2('/ Expense Claim');
                                        }
                                                                             function loadJournelRegister() {
                                            const iframe = document.getElementById("contentIframe");
                                                            iframe.src = "/Finance/JournalRegister";
                                            updateBreadcrumbFromSidebar2('/ Journel Register');
                                        }
        function loadJournelVoucher() {
            const iframe = document.getElementById("contentIframe");
            iframe.src = "/Finance/Voucherjournal";
            updateBreadcrumbFromSidebar2('/ Journel Voucher');
        }


        function updateBreadcrumbFromSidebar2(submenuItem) {
            // Ensure submenuItem is added to the correct position
            if (!breadcrumbTrail.includes(submenuItem)) {
                breadcrumbTrail.push(submenuItem); // Add if it doesn't already exist
            } else {
                // Update the breadcrumb trail to reflect the current selection
                const itemIndex = breadcrumbTrail.indexOf(submenuItem);
                breadcrumbTrail = breadcrumbTrail.slice(0, itemIndex + 1); // Truncate to selected item
            }
            updateBreadcrumb(); // Re-render the breadcrumb trail
        }

               function updateBreadcrumb() {
            const breadcrumbContainer = document.getElementById("breadcrumb");

            // Get existing breadcrumb items
            const existingBreadcrumbItems = breadcrumbContainer.getElementsByTagName("span");

            breadcrumbTrail.forEach((name, index) => {
                // Check if the breadcrumb item already exists
                let breadcrumbItem = existingBreadcrumbItems[index];

                // If the breadcrumb item doesn't exist, create a new one
                if (!breadcrumbItem) {
                    breadcrumbItem = document.createElement("span");
                    breadcrumbContainer.appendChild(breadcrumbItem);
                }

                // Update the breadcrumb item's text
                breadcrumbItem.textContent = name;

                // Allow navigation to a level
                breadcrumbItem.onclick = () => goToLevel(index);

                // Add a separator if not the last item
                if (index < breadcrumbTrail.length - 1) {
                    // Check if separator exists and create it if not
                    let separator = breadcrumbContainer.querySelector(`.separator[data-index="${index}"]`);
                    if (!separator) {
                        separator = document.createElement("span");
                        separator.textContent = " / ";
                        separator.className = "separator";
                        separator.setAttribute('data-index', index);
                        breadcrumbContainer.appendChild(separator);
                    }
                }
            });

            // Remove extra items if the breadcrumb trail is shorter
            for (let i = breadcrumbTrail.length; i < existingBreadcrumbItems.length; i++) {
                breadcrumbContainer.removeChild(existingBreadcrumbItems[i]);
            }
        }

        function goBack() {
            if (breadcrumbTrail.length > 1) {
                breadcrumbTrail.pop(); // Remove the last breadcrumb
                updateBreadcrumb();
            }
        }

        function goToLevel(level) {
            breadcrumbTrail = breadcrumbTrail.slice(0, level + 1); // Keep up to the selected level
            updateBreadcrumb();
        }

        function resetBreadcrumb() {
            breadcrumbTrail = ["Home"]; // Reset to the initial state
            updateBreadcrumb();
        }

        // Example usage to ensure Sidebar2 interactions don't interfere with the trail
        function selectNestedSubmenu(parent, child) {
            updateBreadcrumbFromSidebar2(parent); // Update second level
            updateBreadcrumbFromSidebar2(child); // Add/update the third level
        }
        function autocollaps () {
           
                    const sidebar = document.getElementById('sidebar');
                    const sidebar2 = document.getElementById('sidebar2');
                    const toggleBtn = document.querySelector('.toggle-btn');
                    const root = document.documentElement; // Access CSS custom properties

                    // Toggle visibility of Sidebar 1
                    sidebar.classList.toggle('hidden');
                    sidebar2.classList.toggle('expanded');

                        root.style.setProperty('--sidebar-width', '80px'); // Sidebar 2 width
                        sidebar2.style.marginTop = '70px'; // Adjust Sidebar 2 margin
                               
                  

                           
                    toggleBtn.innerHTML = sidebar.classList.contains('hidden') ? '&gt;' : '&lt;';
        }
        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            const sidebar2 = document.getElementById('sidebar2');
            const toggleBtn = document.querySelector('.toggle-btn');
            const root = document.documentElement; // Access CSS custom properties

            // Toggle visibility of Sidebar 1
            sidebar.classList.toggle('hidden');
            sidebar2.classList.toggle('expanded');

            // Update the CSS variable based on the sidebar state
            if (sidebar.classList.contains('hidden') && sidebar2.classList.contains('expanded')) {
               
                root.style.setProperty('--sidebar-width', '80px'); // Sidebar 2 width
                sidebar2.style.marginTop = '70px'; // Adjust Sidebar 2 margin
            } else if (sidebar.classList.contains('hidden')) {
                       
                root.style.setProperty('--sidebar-width', '0px'); // No sidebar
                sidebar2.style.marginTop = '70px'; // Adjust Sidebar 2 margin
                       
            } else {
                root.style.setProperty('--sidebar-width', '256px'); // Sidebar 1 width
                sidebar2.style.marginTop = '70px'; // Keep Sidebar 2 margin
                sidebar.style.marginTop = '45px'; // Adjust Sidebar 1 margin
                     
            }

            // Adjust the toggle button symbol
            toggleBtn.innerHTML = sidebar.classList.contains('hidden') ? '&gt;' : '&lt;';
        }



                        
    </script>

</body>
</html>
