
<style type="text/css">
    #dataGrid .dx-datagrid-headers .dx-datagrid-action {
        text-align: center !important;
    }

    #dataGrid .dx-datagrid-headers .dx-cell-focus-disabled {
        text-align: center !important;
    }




    .horizontal-container {
        display: flex;
        gap: 10px; 
        justify-content: start; 
        align-items: center; /* Vertically centers the buttons */
        margin: 15px;
        margin-top: 0px;
    }

    .button-alignment-down {
        margin-top: 23px;
    }

    .button-group {
        margin-left: -10px !important;
    }

    .dx-datagrid .dx-row {
        border-bottom: 1px solid #ccc; /* Border between rows */
    }
    /* Custom styles for context menu */
    .context-menu {
        display: none;
        position: absolute;
        background-color: white;
        border: 1px solid #ccc;
        box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
        z-index: 1000;
    }

    .context-menu-item {
        padding: 0px, 15px,10px 15px;
        cursor: pointer;
    }

    .ledgerResult {
        margin: 15px !important;
    }

    #rowHeightMenu {
        position: absolute;
        top: 100px; /* Adjust positioning as per your design */
        left: 300px; /* Adjust positioning as per your design */
        display: none; /* Keep it hidden until needed */
    }

    .custom-button-icon .dx-icon {
        color: #ff5733; /* Set the color of the icon (you can use any color) */
    }

    .context-menu {
        display: none;
        position: absolute;
        background-color: white;
        border: 1px solid #ccc;
        box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
        z-index: 1000;
        width: 200px;
    }

    .context-menu-item {
        padding: 10px;
        cursor: pointer;
        text-align: left;
    }

        .context-menu-item:hover {
            background-color: #f0f0f0;
        }
</style>
<div class="dx-fieldset horizontal-container">

    @* <div id="ddButtonLayout"></div> *@
 @*    @(Html.DevExtreme().Button()
        .ID("btnRefresh")
        .Text("Refresh")
        .Type(ButtonType.Normal)
        .StylingMode(ButtonStylingMode.Outlined)
        .Icon("refresh") // Use the "refresh" icon
        .Width(180)
        .OnClick("function() { refreshPage(); }") // Define a JavaScript function for the click event
        )

 *@
    <div class="btn-container">
    @(Html.DevExtreme().DropDownButton()
        .ID("ddButtonLayout")
        .Text(new JS("formatMessage('Layout')"))
        .Icon("smalliconslayout")
        .Width(180)
        .DropDownOptions(options => options.Width(230))
        .Items(items =>
        {
            items.Add().Text(new JS("formatMessage('Set as Default Layout')")).Icon("lock").OnClick("setDefaultLayoutClick");
            items.Add().Text(new JS("formatMessage('Reset Layout')")).Icon("revert").OnClick("resetLayoutClick");
            items.Add().Text(new JS("formatMessage('Open Layout')")).Icon("activefolder").OnClick("openLayoutClick");
            items.Add().Text(new JS("formatMessage('Save Layout')")).Icon("save").OnClick("saveLayoutClick");
        })
        .ShowArrowIcon(true)

        @* .OnItemClick("addRecipitsButton_click") *@

        )</div>
    <div class="btn-container">
    
        @(Html.DevExtreme().DropDownButton()
                .ID("rowHeightProperties")
                .Icon("") // fallback to standard icon if "rowproperties" not working
                .Width(50)
                .Text(new JS("formatMessage('Set Row Height')"))
                .DropDownOptions(options => options.Width(230))
                .Items(items =>
                {
                items.Add()
                .Text("Short (1 Line)")
                .Template(@<text>
                               <svg width="20" height="20" viewBox="0 0 20 20">
                                   <rect x="2" y="9" width="16" height="2" fill="var(--title-background-color)" />
                               </svg> Short
                           </text>)
                .OnClick("function() { ChangeRowHeight('30'); }");
                items.Add()
                .Text("Medium (2 Lines)")
                .Template(@<text>
                               <svg width="20" height="20" viewBox="0 0 20 20">
                                   <rect x="2" y="6" width="16" height="2" fill="var(--title-background-color)" />
                                   <rect x="2" y="12" width="16" height="2" fill="var(--title-background-color)" />
                               </svg> Medium
                           </text>)
                .OnClick("function() { ChangeRowHeight('60'); }");
                items.Add()
                .Text("Tall (4 Lines)")
                .Template(@<text>
                               <svg width="20" height="20" viewBox="0 0 20 20">
                                   <rect x="2" y="4" width="16" height="2" fill="var(--title-background-color)" />
                                   <rect x="2" y="8" width="16" height="2" fill="var(--title-background-color)" />
                                   <rect x="2" y="12" width="16" height="2" fill="var(--title-background-color)" />
                                   <rect x="2" y="16" width="16" height="2" fill="var(--title-background-color)" />
                               </svg> Tall
                           </text>)
                .OnClick("function() { ChangeRowHeight('90'); }");
                items.Add()
                .Text("Extra Tall (8 Lines)")
                .Template(@<text>
            <svg width="20" height="20" viewBox="0 0 20 20">
                @for (var i = 1; i <= 8; i++)
                        {
                            var y = 1 + (i - 1) * 2;
                            <text><rect x="2" y="@y" width="16" height="1" fill="var(--title-background-color)" /></text>
                        }
            </svg> Extra Tall
        </text>)
                .OnClick("function() { ChangeRowHeight('130'); }");
                })
                .ShowArrowIcon(false))
    
    </div>
    <div class="btn-container">
    @(Html.DevExtreme().Button()
        .ID("btnPrint")
        .Text(new JS("formatMessage('Print')"))
        .Type(ButtonType.Normal)
        .StylingMode(ButtonStylingMode.Outlined)
        .Icon("print")
        .Width(180)
        .OnClick("function() { exportDataGridToPDF('dataGrid'); }")
        )</div>
    <div class="btn-container">
    @(Html.DevExtreme().Button()
        .ID("btnToggleSearch")
        .Text(new JS("formatMessage('Search')"))
        .Type(ButtonType.Normal)
        .StylingMode(ButtonStylingMode.Outlined)
        .Icon("search")
        .Width(180)
        .OnClick("function() { toggleFilterRow(); }")
        )</div>
    <div class="btn-container">
    @(Html.DevExtreme().Button()
        .ID("btntoggleGroupPanel")
        .Text(new JS("formatMessage('Group')"))
        .Type(ButtonType.Normal)
        .StylingMode(ButtonStylingMode.Outlined)
        .Icon("hierarchy")
        .Width(180)
        .OnClick("toggleGroupPanel")
        )</div>
    <div class="btn-container">
    <!-- Show Only with Balance Button -->
    @(Html.DevExtreme().Button()
        .ID("btnShowBalance")
        .Text(new JS("formatMessage('Create Payment Request')"))
        .Type(ButtonType.Normal)
        .StylingMode(ButtonStylingMode.Outlined)
        .Icon("filter")
        .Width("100%")
        .OnClick("function() { toggleBalancePopup(); }")
        )
        </div>
    <div id="balancePopupMenu" class="context-menu" style="display:none;">
        <div id="ViewLedgerTransactions" class="context-menu-item no-border-input"></div>
        <div id="ExporttoPDF" class="context-menu-item no-border-input"></div>
        <div id="ExporttoExcel" class="context-menu-item no-border-input"></div>
        <div id="ExportAccountConfirmationLetter" class="context-menu-item no-border-input"></div>
    </div>

    @*    @(Html.DevExtreme().DropDownButton()
    .ID("ddButtonReceipts")
    .Text("Add Receipts")
    .Icon("add")
    .DropDownOptions(options => options.Width(230))
    .Items(items =>
    {
    items.Add().Text("Add Cash Receipts").Icon("money");
    items.Add().Text("Add Bank Receipts").Icon("product");
    })
    .OnItemClick("addRecipitsButton_click")
    )
    @(Html.DevExtreme().DropDownButton()
    .ID("ddButtonPayment")
    .Text("Add Payment")
    .Icon("add")
    .DropDownOptions(options => options.Width(230))
    .Items(items =>
    {
    items.Add().Text("Add Cash Payment").Icon("money");
    items.Add().Text("Add Bank Payment").Icon("product");
    })
    .OnItemClick("addRecipitsButton_click")
    )
    @(Html.DevExtreme().DropDownButton()
    .ID("ddOtherVouchers")
    .Text("Other Vouchers")
    .Icon("add")
    .DropDownOptions(options => options.Width(230))
    .Items(items =>
    {
    items.Add().Text("Add Purchase Voucher").Icon("add").OnClick("function() { addVoucher_click('Purchase'); }"); ;
    items.Add().Text("Add Sales Voucher").Icon("add").OnClick("function() { addVoucher_click('Sales'); }"); ; ;
    items.Add().Text("Add Journal Voucher").Icon("add").OnClick("function() { addVoucher_click('Journal'); }"); ; ;
    })
    )
    @(Html.DevExtreme().DropDownButton()
    .ID("ddReports")
    .Text("Reports")
    .Icon("doc")
    .DropDownOptions(options => options.Width(230))
    .Items(items =>
    {
    items.Add().Text("Reportss").Icon("money");
    items.Add().Text("Add Sales Voucher").Icon("product");
    })
    .OnItemClick("addRecipitsButton_click")
    ) *@
</div>

<script type="text/javascript">
$(function () {

										$("#ExportAccountConfirmationLetter").dxTextBox({
													text:formatMessage("Export Account Confirmation Letter"),
                                                        readOnly: true
		

                                    });
									});
$(function () {

										$("#ExporttoExcel").dxTextBox({
													text:formatMessage("Export to Excel"),
                                                        readOnly: true
		

                                    });
									});
$(function () {

										$("#ExporttoPDF").dxTextBox({
													text:formatMessage("Export to PDF"),
                                                        readOnly: true
		

                                    });
									});
$(function () {

										$("#ViewLedgerTransactions").dxTextBox({
													text:formatMessage("View Ledger Transactions"),
                                                        readOnly: true
		

                                    });
									});
    // Hide context menu on click outside
    $(document).on("click", function () {
        var contextMenu = $("#contextMenu");
        contextMenu.hide();
        var $submenu = $('#submenuContainer');
        $submenu.hide(); // Hide the submenu popup
    });

    // Handle the file selection

    // Footer Recipits button event
    function addRecipitsButton_click(e) {
        DevExpress.ui.notify(e.itemData, "success", 600);
    }
   
    function showSubmenu(e) {
        e.event.stopPropagation();
        var $submenu = $('#submenuContainer');
        var $target = $(e.event.target).closest('.dx-item-content'); // Target the content of the item
        var offset = $target.offset();
        var elementWidth = $target.outerWidth();
        var elementHeight = $target.outerHeight();

        // Position the submenu to the right of the dropdown item
        $submenu.css({
            'width': 180,
            'position': 'absolute',
            'top': offset.top,
            'left': offset.left + elementWidth, // Position to the right of the item
            'display': 'block' // Show the submenu
        });
    }
    function closeSubmenu() {
        var $submenu = $('#submenuContainer');
        $submenu.hide(); // Hide the submenu popup
    }
   
    function toggleBalancePopup() {
        var popupMenu = document.getElementById('balancePopupMenu');
        // Toggle the display of the balance popup menu
        if (popupMenu.style.display === 'none' || popupMenu.style.display === '') {
            popupMenu.style.display = 'block';
        } else {
            popupMenu.style.display = 'none';
        }
    }
 
</script>

